## SECTION 1: CORRECTNESS

### Main user flow walkthrough

#### 1) Media page loads
- `templates/media_unified.html` renders a large dashboard with telemetry, feeds, gauge, newsletter, etc.
- Initial SSR content exists for some sections (`latest_episodes`, `ssr_highlights`, `series_list`, `all_books`), so the page won’t be blank if JS fails. That’s good.

#### 2) Frontend boot sequence
- On `DOMContentLoaded`, the inline runtime calls:
  - `updateTelemetry()` immediately and every 30s (`templates/media_unified.html:793-797`)
  - `syncRelayStatusBar()` every 5s (`798-799`)
  - `updateHealthStrip()` immediately and every 60s (`801-803`)

This means the “live” sentiment/X Spaces behavior is implemented via polling, not SSE.

#### 3) Sentiment fetch
- `fetchSentiment()` calls `/api/media/sentiment` and caches the JSON (`590-600`).
- If fetch fails, it returns cached data or `{ composite_score: null, label: 'OFFLINE' }`.

**Correctness issue:** the code assumes the sentiment payload has `composite_score`, but the governing laws require per-article sentiment classification and real-time streaming of new classifications. This page is consuming an aggregate endpoint, not article-level classification events. There is no evidence here that new article badges appear when classification completes.

#### 4) X Spaces fetch
- `fetchSpaces()` calls `/api/spaces/live` (`602-612`).
- `updateXSpacesTelemetry()` updates score/label and health dot (`703-728`).

This is functionally okay for a telemetry widget, but again it is polling.

#### 5) Signal strength gauge
- `computeSignalStrength()` computes:
  - sentiment score from `sentData.composite_score`
  - spaces score from `spacesData.spaces.length * 10`, capped at 100 (`626-633`)
- `renderSignalGauge()` then renders the gauge and breakdown (`635-655`).

**Logic bug:** `renderSignalGauge()` expects `spacesScore` to be a score, but `updateTelemetry()` passes `spacesCount` instead:
- `spacesCount = spacesData.spaces.length` (`745`)
- `renderSignalGauge(score, sentScore, spacesCount)` (`748`)
- Inside renderer, breakdown does `Math.min((spacesScore||0)*10,100)` (`653`)

So the displayed X Spaces breakdown is derived from count *again*, while `computeSignalStrength()` already did that conversion. The naming is misleading and easy to break. It happens to display the same intended value only because of this double convention mismatch. This is fragile and not clean.

#### 6) Relay status bar
- `syncRelayStatusBar()` inspects `window.relayManager.sockets` and `window.state.nostrNotes` (`659-700`).

**Potential runtime fragility:**
- It assumes `window.relayManager.sockets` is an object of WebSocket-like instances with `readyState`.
- It assumes `window.state.nostrNotes` is an array.
- It silently returns if not present, so the UI may remain permanently stale with no user-facing error.

**Minor bug:** `countEl` is queried at `669` but not used in the first half of the function.

#### 7) Health strip
- `updateHealthStrip()` HEAD-checks several services (`755-790`).

**Correctness / compatibility issue:**
- It uses `fetch(..., { method: 'HEAD' })` for all services (`767`).
- Many app endpoints do not implement HEAD correctly, especially JSON routes like `/api/spaces/live` and `/api/tradfi/signals`.
- This can falsely mark healthy services as DOWN/DEGRADED.

#### 8) Newsletter subscribe
- `subscribeNewsletter()` POSTs raw email JSON to `/api/newsletter/subscribe` (`468-480`).

**Correctness/security concern:** only client-side validation is `email.includes('@')` (`470`). That is not meaningful validation. If backend is weak, junk input will flood the endpoint.

---

### TTS modules walkthrough

These two Python files are not sentiment-intel code. They appear unrelated to the feature under review.

#### `video_pipeline_v3/dual_host_tts.py`
- Generates line-by-line TTS via ElevenLabs.
- Fallback chain: ElevenLabs → `pyttsx3` → silence.
- Concatenates output via ffmpeg.

**Critical correctness bug:** `_mp3_to_m4a()` is used to convert a WAV file generated by `pyttsx3`:
- WAV temp created at `209-211`
- Then `_mp3_to_m4a(wav_tmp, output_path)` called at `213`

Despite the function name, ffmpeg can still convert WAV input because it just uses `-i <path>`. So this is ugly naming, not necessarily broken.

**More serious bug:** `generate_dialogue_audio()` hard-fails if `ELEVENLABS_API_KEY` is missing:
- `277-279` raises `RuntimeError`
- But `tts_elevenlabs()` itself already supports fallback to silence when key is missing (`151-153`)
- So the top-level function defeats its own graceful degradation path.

Same issue exists in `video_pipeline_v3/tts_engine.py`:
- `311-313` raises before fallback can happen.

**Timing bug with CLIP entries in both TTS files:**
- In `dual_host_tts.py`, CLIP entries append metadata with `duration=clip_dur` but do **not** advance `current_time` (`292-303`).
- In `tts_engine.py`, CLIP entries are recorded with `duration: 0.0` and also do **not** advance `current_time` (`326-337`).

This means all subsequent `start` timestamps are wrong whenever a CLIP marker exists. That will break subtitle sync, lip-sync alignment, timeline UI, or any downstream editing logic.

---

## SECTION 2: LAW COMPLIANCE

### LAW 1: Sentiment is calculated from real articles — never fake or static
**Status: VIOLATION**

What is required:
- Every new article gets sentiment classified within 60s
- Batch re-classify last 100 articles on restart
- Use `claude-haiku-4-5`
- Store in `articles`: `sentiment`, `sentiment_confidence`, `sentiment_at`

What is present in reviewed code:
- Frontend polls `/api/media/sentiment` (`590-600`)
- No article classification pipeline shown
- No DB schema/storage shown
- No restart catch-up shown
- No model selection shown
- No evidence of per-article sentiment fields

There is nothing in these files demonstrating compliance.

### LAW 2: SSE for real-time sentiment stream — not polling
**Status: VIOLATION**

Required:
- `/api/stream/sentiment` SSE endpoint
- Push on every new classification
- Browser shows new sentiment badge appearing on article as classified
- Smooth CSS fade-in animation on new sentiment badges

Actual:
- Polling every 30s via `setInterval(updateTelemetry, 30000)` (`795-797`)
- No `EventSource`
- No `/api/stream/sentiment`
- No article badge insertion logic
- No fade-in animation for sentiment badges

Direct violation.

### LAW 3: Narrative intelligence is the key differentiator
**Status: VIOLATION**

Required:
- Extract narrative labels from article body
- Not just bullish/bearish

Actual:
- UI shows generic sentiment score/label and X Spaces label
- Verified highlights only show `direction == 'bullish'/'bearish'` (`192`)
- No narrative extraction, no narrative labels, no article-body intelligence

Direct violation.

### LAW 4: Anomaly detection fires loud
**Status: VIOLATION**

Required:
- Detect >20 point sentiment shift in 2 hours
- Log anomaly
- Store in `intelligence_events`
- Show dashboard banner alert with timestamp

Actual:
- No anomaly detection logic
- No banner alert component
- No intelligence events rendering
- No timestamped anomaly UI

Direct violation.

---

## SECTION 3: SECURITY

### Findings

#### 1) Missing CSRF protection on newsletter subscribe
- `fetch('/api/newsletter/subscribe', { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({email: email}) })`
- `templates/media_unified.html:471-475`

If the backend does not enforce CSRF tokens or same-site protections, this endpoint is CSRFable.

#### 2) No client-side throttling / abuse control for paid endpoints
- Page polls `/api/media/sentiment`, `/api/spaces/live`, `/api/tradfi/signals` every 30s (`731-736`, `795-797`)
- Health strip also hits multiple endpoints every 60s (`776-790`, `801-803`)

At ~1000 concurrent users, this creates a large fan-out. If any backend endpoint triggers paid upstream calls, one page can amplify cost. There is no visible rate limiting, caching strategy, or SSE fanout architecture here.

#### 3) Potential HTML injection risk avoided in most places, but inline HTML assembly is still risky
- `renderSignalGauge()` uses `innerHTML` (`640-647`)
- `updateHealthStrip()` uses `innerHTML` with service names and computed values (`780-789`)

In this specific code, inserted values are locally controlled, so immediate XSS risk is low. But this pattern is dangerous if later fed API strings.

#### 4) Shell invocation review
- Python uses `subprocess.run([...])`, not shell strings, which is good.
- No obvious shell injection in reviewed Python files.

#### 5) Secrets in code
- No hardcoded API keys found.
- Voice IDs are hardcoded, but those are identifiers, not secrets.

#### 6) Filesystem input
- TTS output paths are derived from internal code, not user input. Low risk in reviewed snippets.

### Security summary
No obvious catastrophic injection bug in the provided snippets, but there are meaningful abuse/CSRF concerns and architectural cost-exhaustion risk.

---

## SECTION 4: FRONTEND QUALITY

### Does UI match spec?
For the sentiment-intel feature: **no**.

#### Major spec misses
- No SSE sentiment stream
- No article-level sentiment badges appearing in real time
- No fade-in animation for new sentiment badges
- No narrative labels
- No anomaly banner

#### Hardcoded / prototype-like elements
- Library leaderboard is static hardcoded content (`325-360`)
- Rising stars are hardcoded (`365-367`)
- Learning paths are hardcoded (`375-395`)
- Health strip service list is hardcoded (`755-761`)
- Relay status bar has hardcoded relays (`155-173`)

This may be acceptable for unrelated sections, but it does make the page feel partly prototype-ish.

#### Spec violation on rendering technology
The stack rules say:
- “All UI animations: CSS/SVG only — NO Three.js, no WebGL, no Canvas”

But the template includes `<canvas>` sparklines:
- `24`, `33`, `42`

Even if not animated here, this appears to violate the stated UI technology constraint.

#### Mobile / viewport concerns
- Telemetry ribbon is very dense with many inline metrics (`19-95`)
- Health strip is fixed bottom (`550-557`)
- Signal gauge layout uses `flex-wrap`, which helps (`506-509`)
- But there is no evidence of responsive handling in this file for the telemetry ribbon density, command hint, sentiment track, and health dots all together.

Likely cramped on mobile unless external CSS handles it very well.

#### Loading / error / empty states
Mixed quality:
- Some async calls degrade to cached/offline values (`596-599`, `609-610`)
- Health strip always renders something (`779-789`)
- But there are no explicit user-facing error states for failed sentiment/spaces/tradfi fetches beyond stale values.
- No empty state for sentiment narratives/anomalies because those features don’t exist.

#### World-class feel?
Visually ambitious, yes.
Operationally/spec-wise, no.
It reads like a broad dashboard prototype with premium styling, not a finished intelligence product.

---

## SECTION 5: BACKEND QUALITY

Only two backend Python files are provided, and they are TTS-related, not sentiment-intel.

### `dual_host_tts.py`
#### Good
- External API calls have timeout (`178`)
- Retries on failure / 429 (`176-195`)
- ffmpeg calls use argument arrays, not shell strings
- Temporary chunk cleanup exists (`198-202`, `245-250`)

#### Problems
1. **Graceful degradation is broken at top level**
   - `generate_dialogue_audio()` raises if key missing (`277-279`)
   - This bypasses fallback behavior in `tts_elevenlabs()`

2. **CLIP timing metadata broken**
   - `current_time` not advanced for CLIP duration (`292-303`)

3. **No timeout on some subprocess calls**
   - `ffprobe_duration()` has no timeout (`81-85`)
   - concat ffmpeg run has no timeout (`239-243`, `342-346`)
   - Hung ffmpeg/ffprobe can stall worker processes

4. **Logging is print-based**
   - No structured logging, no request/job IDs, no severity levels

### `tts_engine.py`
Same issues, plus:
1. **Top-level key check defeats fallback**
   - `311-313`

2. **CLIP duration ignored entirely**
   - CLIP lines stored as `duration: 0.0` (`328-336`)
   - `current_time` not advanced (`327-337`)

3. **No locking around cache writes**
   - `_tts_cache_put()` writes to shared cache path without file lock (`131-138`)
   - Concurrent workers can race on same cache file

4. **No timeout on ffprobe / concat ffmpeg**
   - `61-66`, `278-282`, `380-384`

### DB operations / rollback / cron / indexes
Not reviewable from provided code. No sentiment backend code was included, which itself is a major audit gap for this feature.

---

## SECTION 6: WORLD-CLASS GAP ANALYSIS

Material gaps only:

1. **No event-driven sentiment architecture**
   - A premium intelligence product would use backend classification workers + SSE/WebSocket fanout + article-level updates, not 30s polling.

2. **No narrative layer**
   - This is the feature’s differentiator and it is absent. Professionals need “why sentiment moved,” not just a score.

3. **No anomaly intelligence surface**
   - A serious terminal would elevate regime shifts immediately with timestamps, deltas, and linked source articles.

4. **No provenance / explainability**
   - There is no visible “derived from X articles / top narratives / confidence / last updated” context. Bloomberg-style products show source-backed reasoning.

5. **Frontend is broad, not deep**
   - The page has many sections, but the sentiment-intel feature itself is shallow. A professional user would prefer fewer modules with stronger intelligence depth.

What is already good:
- The page has strong visual ambition and decent resilience for some fetch failures.
- The TTS modules show practical retry/fallback thinking, even though execution has flaws.

---

## SECTION 7: SCORES (0-100 each)

- Backend logic:    42/100
- Frontend/UI:      58/100
- Error handling:   49/100
- Security:         68/100
- Performance:      45/100
- Law compliance:   8/100
- World-class gap:  22/100
- OVERALL:          39/100

---

## SECTION 8: PRIORITY ACTION PLAN

P0 CRITICAL | Replace 30s polling with required SSE sentiment stream and article-level badge updates | templates/media_unified.html:731-752, 793-797 | Law 2 explicitly requires `/api/stream/sentiment` SSE; current implementation cannot deliver real-time classification UX and is a direct spec failure

P0 CRITICAL | Implement actual article sentiment pipeline with DB persistence fields and restart catch-up | feature backend not present in provided code | Law 1 requires real article classification within 60s, restart reclassification of last 100, and storage in `articles`; none of this is evidenced here, so the feature is fundamentally incomplete

P0 CRITICAL | Add narrative extraction from article body and surface narrative labels in UI | templates/media_unified.html:74-84, 188-193 and missing backend | Law 3 is the product differentiator and is entirely absent; without it this is not the requested feature

P0 CRITICAL | Implement anomaly detection, persistence to `intelligence_events`, and dashboard alert banner | templates/media_unified.html entire page, missing backend | Law 4 is entirely missing; no anomaly logic, no event storage, no alerting

P1 HIGH     | Fix TTS timeline corruption by advancing `current_time` for CLIP durations | video_pipeline_v3/dual_host_tts.py:292-303; video_pipeline_v3/tts_engine.py:326-337 | Downstream sync metadata becomes wrong whenever clips exist, breaking editing/subtitle/lip-sync alignment

P1 HIGH     | Remove top-level ElevenLabs key hard-fail so fallback chain can actually work | video_pipeline_v3/dual_host_tts.py:277-279; video_pipeline_v3/tts_engine.py:311-313 | Current code claims graceful degradation but crashes before fallback when key is missing

P1 HIGH     | Replace HEAD-based health checks with endpoint-specific lightweight GET or dedicated `/health` routes | templates/media_unified.html:763-773 | Many JSON endpoints do not support HEAD correctly, causing false DOWN states in production

P1 HIGH     | Add server-side rate limiting/caching for polled endpoints or, preferably, consolidate via SSE fanout | templates/media_unified.html:731-736, 776-790, 795-803 | At 1000 concurrent users this polling pattern can amplify backend and paid upstream load significantly

P1 HIGH     | Remove or replace `<canvas>` sparklines if UI law forbids Canvas | templates/media_unified.html:24, 33, 42 | Governing stack explicitly says no Canvas; current template appears non-compliant

P2 MEDIUM   | Replace `print()` logging with structured logging including job/context IDs | video_pipeline_v3/dual_host_tts.py:138, 186, 189, 193, 204, 327, 353; video_pipeline_v3/tts_engine.py:153, 178, 220, 223, 227, 238, 253, 364, 391 | Production debugging will be painful without structured logs

P2 MEDIUM   | Add timeouts to `ffprobe` and concat `ffmpeg` subprocess calls | video_pipeline_v3/dual_host_tts.py:81-85, 239-243, 342-346; video_pipeline_v3/tts_engine.py:62-66, 278-282, 380-384 | Hung media subprocesses can stall workers indefinitely

P2 MEDIUM   | Add file locking or atomic temp-rename semantics around shared TTS cache writes | video_pipeline_v3/tts_engine.py:131-138 | Concurrent workers can race and corrupt cache behavior

P2 MEDIUM   | Improve newsletter input validation and ensure CSRF protection | templates/media_unified.html:468-480 | Current validation is trivial and endpoint may be abuse-prone if backend protections are weak

P2 MEDIUM   | Stop building HTML with `innerHTML` where textContent-based DOM updates suffice | templates/media_unified.html:640-647, 780-789 | Low immediate risk here, but this pattern increases future XSS exposure

P3 LOW      | Clean up misleading variable naming around spaces score/count | templates/media_unified.html:626-655, 745-748 | Current implementation works by convention but is confusing and fragile

P3 LOW      | Remove unused local `countEl` assignment in relay sync | templates/media_unified.html:669 | Minor code hygiene issue

P3 LOW      | Move large inline CSS/JS blocks out of template into versioned static assets | templates/media_unified.html:485-807 | Improves maintainability, caching, and reviewability

---

## SECTION 9: THE ONE THING

Build the sentiment feature as an event-driven article intelligence pipeline with SSE, narratives, and anomaly events—right now the code is mostly a dashboard shell without the core product logic the spec demands.

---

## SECTION 10: FINAL VERDICT

This is **not ready for production** as the `p3-sentiment-intel` feature. The biggest problem is not polish—it’s that the core required functionality (real article classification, SSE streaming, narrative extraction, anomaly detection, and required persistence) is missing or contradicted by the current polling-based UI.