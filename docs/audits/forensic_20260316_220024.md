# Pipeline Forensic Audit 2026-03-16 22:00:24.756722


---
## Grok3
GROK_ERROR:HTTP Error 403: Forbidden

---
## GPT4o
GPT4_ERROR:HTTP Error 429: Too Many Requests

---
## Gemini
Alright. Coffee's black, no sugar. Let's rip this thing apart.

This isn't a pipeline; it's a Rube Goldberg machine held together with duct tape and wishful thinking. The Grade F is a mercy killing. You have a cascade failure originating from a single, fundamental flaw: **your system is schizophrenic about its single-host identity.**

Let's get straight to the point.

### 1. Why is it silent despite `DUAL_HOST_ENABLED=False`?

Because that flag is a lie. It's a placebo. Your code is still executing legacy dual-host logic paths.

The setting `DUAL_HOST_ENABLED=False` is clearly not respected by the component that matters. The core failure is that something, somewhere, is still attempting to generate audio for `host: 1`. When it does, it's not using the `_PBX_VOICE` config from `tts_engine.py`; it's pulling a **ghost configuration** from a dead environment variable, a stale config file, or a hardcoded value in a function I can't see.

The `VOICES` dictionary in `tts_engine.py` (lines 37-40) is a weak attempt to paper over this. You've pointed both `1` and `2` to the correct PBX voice, but the actual TTS call for `host: 1` is clearly ignoring this dictionary and using its own cursed logic to find a voice ID.

The silence is the *result* of this failure. The HTTP 404 on the invalid voice ID causes the TTS generation to fail, and your misguided fallback logic in `assembler.py` (line 4722) dutifully fills the gap with silent audio instead of crashing the pipeline like it should.

### 2. Where does `voice_id uxKr2vlA4hYgXZR1oPRT` originate?

It's a ghost. A phantom from a previous version of this pipeline.

That ID is not in any of the code you've shown me. This means it's coming from an external configuration that has not been properly decommissioned. My money is on one of these:

1.  **An environment variable:** `HOST_1_VOICE_ID` or `DEBORAH_VOICE_ID` is still set in your execution environment (`.env`, CI/CD variables, Kubernetes configmap, etc.). The unseen `tts_elevenlabs` function has a legacy branch like `if host == 1: voice_id = os.environ.get(...)` that takes precedence over your new `VOICES` dictionary.
2.  **A stale config file:** A `config.json` or `.yaml` file is being read somewhere that contains the old voice mapping.
3.  **A database entry:** Less likely, but possible there's a `voices` table somewhere that's being queried.

The `script_writer.py` prompt is practically screaming about this problem. The frantic instructions to the LLM ("NEVER output 'host': 1", "catastrophic render failure") prove the engineers knew this was a fragile, un-fixed bug and tried to solve it with prompt engineering instead of fixing the code.

### 3. What is the root cause of the black frames and silence?

**The root cause is a TTS API failure (HTTP 404) being handled by a fatally flawed fallback mechanism.**

Here is the exact chain of events:

1.  **LLM Hallucination:** The LLM, despite your desperate pleading in `script_writer.py`, generates a script with a dialogue entry that is either `{"host": 1, ...}` or, more likely, is missing the `"host"` key entirely.
2.  **Unsafe Default:** The `assembler.py` script on line 4731 executes this catastrophic line: `host_num = int(line_audio.get("host", 1)) ...`. It **defaults to `1`** if the host key is missing. This is the trigger.
3.  **Ghost Config:** The request to generate TTS for `host_num = 1` hits the legacy code path, which fetches the dead voice ID `uxKr2vlA4hYgXZR1oPRT`.
4.  **API Failure:** The call to ElevenLabs fails with an HTTP 404. No MP3 file is returned.
5.  **Failure Masking:** Instead of halting the process, your `assembler.py` script catches this failure. The logic around line 4722 (`_generate_fallback_silent_audio`) is explicitly designed to "fix" this by generating a silent audio clip of equivalent length. This happens 40 times.
6.  **Video Desync:** The video assembly stage expects an audio clip for every dialogue line. It receives 40 silent clips. When it tries to mux these with the corresponding video segments (which may not even exist if their generation depends on audio timing), you get black frames, frozen frames, and a timeline full of gaps. The video assembler is trying to build a house on a foundation of sand.

Your pipeline is designed to **fail silently**, which is the most dangerous anti-pattern in any automated system. It prioritizes finishing a garbage render over reporting a critical error.

---

### 4. P0 Bugs: File, Line, and Fix

This is a target-rich environment.

#### **P0 Bug #1: Unsafe Host Default**

*   **File:** `assembler.py`
*   **Line:** 4731
*   **Problem:** Defaulting a missing host to `1` is the direct trigger for this entire mess. In a system that is supposed to be single-host, this is unforgivable.
*   **Fix:** **Change the default to the ONLY valid host.**
    ```diff
    - host_num = int(line_audio.get("host", 1)) if str(line_audio.get("host", "1")).isdigit() else 1
    + # PBX is host 2. There are no other hosts. Default to the only valid option.
    + host_num = int(line_audio.get("host", 2))
    ```
    Better yet, validate the script after the LLM generates it. If any host is not `2`, fail the run immediately.

#### **P0 Bug #2: Catastrophic "Fallback to Silence" Logic**

*   **File:** `assembler.py`
*   **Lines:** 405-424 (`_generate_fallback_silent_audio`), 4722-4729 (the call site).
*   **Problem:** This function is a landmine. It intentionally hides upstream failures, guaranteeing a Grade F output instead of a clean crash. It is the sole reason you have 40 silence gaps instead of a single, clear error message.
*   **Fix:** **DELETE IT.**
    The TTS generation function (`tts_elevenlabs`) must raise an exception on any non-200 API response. The main assembly loop must not catch this exception. Let the pipeline burn. A fast, loud failure is a thousand times more valuable than a slow, silent one.

#### **P0 Bug #3: Ghost Configuration / Legacy Code Path**

*   **File:** `tts_engine.py` (specifically, the unseen `tts_elevenlabs` function)
*   **Line:** N/A (in hidden code)
*   **Problem:** The `tts_elevenlabs` function is not stateless. It is reading configuration from an unknown, external source instead of relying *only* on the `VOICES` dictionary passed to it.
*   **Fix:** **Perform a surgical excision.**
    1.  Find the `tts_elevenlabs` function.
    2.  Delete any and all code that reads `os.environ`, `config.get`, or any other configuration mechanism.
    3.  The function must be refactored to derive ALL of its parameters (voice_id, stability, model, etc.) exclusively from the `VOICES[host_num]` dictionary lookup. It should be impossible for it to use a voice ID not explicitly defined in that dictionary on line 37.

#### **P0 Bug #4: Brittle LLM Guardrail**

*   **File:** `script_writer.py`
*   **Line:** 179-180
*   **Problem:** You are trying to fix a data integrity problem *after* the fact. The `for` loop to normalize `host:1` to `host:2` is a patch, not a solution. It doesn't account for missing `host` keys, which is the more likely failure mode.
*   **Fix:** **Implement rigorous schema validation.**
    After receiving the JSON from the LLM and before doing anything else, validate it against a strict schema (e.g., Pydantic).
    ```python
    # In script_writer.py, after getting the LLM response
    from pydantic import BaseModel, Field
    from typing import List, Union

    class DialogueEntry(BaseModel):
        host: Union[int, str] = Field(..., regex=r"^(2|CLIP)$") # MUST be 2 or "CLIP"
        text: str
        type: str
        # ... other fields

    class Script(BaseModel):
        dialogue: List[DialogueEntry]
        # ... other top-level fields

    try:
        Script.model_validate(llm_json_output)
    except ValidationError as e:
        logger.critical(f"LLM output failed schema validation: {e}")
        raise  # Fail the entire run
    ```
    This ensures that by the time the script gets to the assembler, its structure is guaranteed. Stop asking the LLM nicely and start enforcing the rules with code.

Fix these four P0s and this pipeline might just limp its way to a passing grade. Leave them, and you'll be auditing this same failure again next week. Get to work.
