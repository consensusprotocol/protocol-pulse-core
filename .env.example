# Protocol Pulse – copy to .env and fill in values

# Required for session security (use a long random string in production)
SESSION_SECRET=your_secret_here

# Database (default SQLite)
DATABASE_URL=sqlite:///protocol_pulse.db

# Server port
PORT=5000

# --- Optional: API keys (app runs without them; features are disabled if missing) ---

# XAI / Grok
XAI_API_KEY=

# OpenAI (e.g. image generation)
OPENAI_API_KEY=

# Google Gemini
GEMINI_API_KEY=

# Printful merch store – get from https://developers.printful.com/ (Dashboard → Create token)
# Required for /merch to show your Printful products and for checkout.
PRINTFUL_API_KEY=

# X (Twitter) API – required for posting tweets and for Spaces search (live Spaces → tweet feature).
# Get from https://developer.x.com/ (Basic tier ~$200/mo for Spaces). Copy from your secret store into .env.
# TWITTER_API_KEY=
# TWITTER_API_SECRET=
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_TOKEN_SECRET=
# TWITTER_BEARER_TOKEN=
