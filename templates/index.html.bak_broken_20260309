{% extends "base.html" %}

{% block title %}Protocol Pulse — Bitcoin Intelligence for Transactors{% endblock %}
{% block meta_description %}Real-time Bitcoin intelligence: market signals, investigative reporting, and deep-protocol analysis. Built for people who actually use Bitcoin.{% endblock %}

{% block styles %}
<style>
/* ═══════════════════════════════════════
   HERO SECTION
   ═══════════════════════════════════════ */
.pp-hero {
    position: relative;
    overflow: hidden;
    padding: 140px 0 80px;
    background:
        radial-gradient(ellipse at 20% 50%, rgba(220,38,38,0.08) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 20%, rgba(245,166,35,0.06) 0%, transparent 50%),
        var(--pp-black);
}
.pp-hero__orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
    opacity: 0.4;
}
.pp-hero__orb--1 { width: 600px; height: 600px; background: rgba(220,38,38,0.12); top: -200px; right: -200px; }
.pp-hero__orb--2 { width: 400px; height: 400px; background: rgba(245,166,35,0.08); bottom: -100px; left: -100px; }
.pp-hero__orb--3 { width: 300px; height: 300px; background: rgba(220,38,38,0.04); top: 50%; left: 50%; transform: translate(-50%,-50%); }
.pp-hero__content { position: relative; z-index: 1; max-width: 680px; }
.pp-hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 16px;
    background: rgba(220,38,38,0.1);
    border: 1px solid rgba(220,38,38,0.25);
    border-radius: 20px;
    font-family: var(--pp-font-mono);
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--pp-red, #CC2222);
    text-transform: uppercase;
    margin-bottom: 24px;
}
.pp-hero__title {
    font-size: clamp(2.5rem, 5vw, 3.75rem);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--pp-text);
    margin-bottom: 20px;
}
.pp-hero__title-accent {
    background: linear-gradient(135deg, #f5a623 0%, #ff6b35 50%, #dc2626 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}
.pp-hero__subtitle {
    font-size: 1.125rem;
    color: var(--pp-text-secondary);
    line-height: 1.7;
    margin-bottom: 36px;
    max-width: 520px;
}
.pp-hero__actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-bottom: 28px;
}
.pp-hero__btn-stage {
    border-color: rgba(220,38,38,0.3);
    color: var(--pp-red, #CC2222);
}
.pp-hero__price-widget {
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 16px 20px;
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: var(--pp-radius);
    backdrop-filter: blur(10px);
    width: fit-content;
    margin-top: 8px;
}
.pp-hero__price-label {
    font-family: var(--pp-font-mono);
    font-size: 0.6875rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    color: var(--pp-text-muted);
    text-transform: uppercase;
    margin-bottom: 4px;
}
.pp-hero__price-value {
    font-family: var(--pp-font-mono);
    font-size: 2rem;
    font-weight: 700;
    color: var(--pp-text);
    line-height: 1;
}
.pp-hero__price-change {
    font-family: var(--pp-font-mono);
    font-size: 0.875rem;
    font-weight: 700;
    padding: 4px 10px;
    border-radius: var(--pp-radius-sm);
}
.pp-hero__price-change--up { background: rgba(245,166,35,0.12); color: var(--pp-amber); }
.pp-hero__price-change--down { background: rgba(220,38,38,0.1); color: var(--pp-red, #CC2222); }
.pp-hero__price-chart { opacity: 0.8; }
.pp-hero__pulse-line {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent 0%, rgba(245,166,35,0.3) 30%, rgba(220,38,38,0.4) 50%, rgba(245,166,35,0.3) 70%, transparent 100%);
}

/* ═══════════════════════════════════════
   NEW HERE PATH TO VALUE
   ═══════════════════════════════════════ */
.pp-hero__new-here {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: -16px;
    margin-bottom: 24px;
}
.pp-hero__new-here-pill {
    display: inline-flex;
    align-items: center;
    padding: 3px 10px;
    background: rgba(245,166,35,0.1);
    border: 1px solid rgba(245,166,35,0.25);
    border-radius: 20px;
    font-family: var(--pp-font-mono);
    font-size: 0.625rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    color: var(--pp-amber);
    text-transform: uppercase;
    white-space: nowrap;
}
.pp-hero__new-here-text {
    font-size: 0.875rem;
    color: rgba(255,255,255,0.4);
    line-height: 1.5;
}
.pp-hero__new-here-link {
    color: var(--pp-amber);
    text-decoration: none;
    font-weight: 600;
    border-bottom: 1px solid rgba(245,166,35,0.3);
    transition: border-color 0.15s ease, opacity 0.15s ease;
}
.pp-hero__new-here-link:hover { opacity: 0.8; border-bottom-color: rgba(245,166,35,0.6); }
.pp-hero__new-here-link--stage { color: var(--pp-red, #CC2222); opacity: 0.8; }
.pp-hero__new-here-sep { margin: 0 6px; opacity: 0.3; }

/* ═══════════════════════════════════════
   STATS ROW — LIVE NETWORK DATA
   ═══════════════════════════════════════ */
.pp-stat--loading .pp-stat__value { opacity: 0.4; }
.pp-stat__value--live { transition: all 0.4s ease; }

/* ═══════════════════════════════════════
   ORACLE SECTION
   ═══════════════════════════════════════ */
.pp-oracle-card {
    position: relative;
    overflow: hidden;
    border-radius: var(--pp-radius-lg);
    border: 1px solid rgba(245,166,35,0.2);
    background:
        radial-gradient(ellipse at 20% 50%, rgba(245,166,35,0.08) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 30%, rgba(220,38,38,0.06) 0%, transparent 50%),
        linear-gradient(135deg, #0a0a0a 0%, #111 100%);
}
.pp-oracle-card__scanlines {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: repeating-linear-gradient(
        0deg,
        transparent,
        transparent 2px,
        rgba(245,166,35,0.015) 2px,
        rgba(245,166,35,0.015) 4px
    );
}
.pp-oracle-card__inner {
    display: flex;
    align-items: center;
    gap: 32px;
    padding: 40px;
    flex-wrap: wrap;
}
.pp-oracle-card__avatar {
    flex-shrink: 0;
    width: 180px;
    height: 180px;
    border-radius: 50%;
    border: 2px solid rgba(245,166,35,0.3);
    box-shadow: 0 0 40px rgba(245,166,35,0.1), 0 0 80px rgba(220,38,38,0.05);
    overflow: hidden;
    background: #0a0a0a;
    position: relative;
}
.pp-oracle-card__avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: contrast(1.1) brightness(1.05);
}
.pp-oracle-card__pulse-ring {
    position: absolute;
    inset: -4px;
    border-radius: 50%;
    border: 2px solid rgba(245,166,35,0.15);
    animation: pp-oracle-pulse 3s ease-in-out infinite;
}
.pp-oracle-card__body { flex: 1; min-width: 260px; }
.pp-oracle-card__badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 12px;
    background: rgba(245,166,35,0.08);
    border: 1px solid rgba(245,166,35,0.2);
    border-radius: 20px;
    font-family: var(--pp-font-mono);
    font-size: 0.625rem;
    font-weight: 700;
    color: var(--pp-amber);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 16px;
}
.pp-oracle-card__badge-dot {
    display: inline-block;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--pp-amber);
    animation: pp-pulse-dot-glow 2s infinite;
}
.pp-oracle-card__title {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 700;
    color: var(--pp-text);
    line-height: 1.2;
    margin-bottom: 10px;
}
.pp-oracle-card__title-gradient {
    background: linear-gradient(135deg, #f5a623 0%, #ff6b35 50%, #dc2626 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}
.pp-oracle-card__desc {
    font-size: 0.9375rem;
    color: var(--pp-text-secondary);
    line-height: 1.7;
    margin-bottom: 20px;
    max-width: 420px;
}
.pp-oracle-card__actions { display: flex; gap: 10px; flex-wrap: wrap; }
.pp-oracle-card__btn-primary {
    background: linear-gradient(135deg, var(--pp-amber), #ff6b35);
    border: none;
    color: #000;
    font-weight: 700;
}
.pp-oracle-card__btn-ghost { border-color: rgba(245,166,35,0.3); color: var(--pp-amber); }

/* ═══════════════════════════════════════
   PULSE CHECK SECTION
   ═══════════════════════════════════════ */
.pp-pulse-check-section {
    background: var(--pp-surface);
    border-top: 1px solid var(--pp-border);
    border-bottom: 1px solid var(--pp-border);
}
.pp-video-embed {
    position: relative;
    aspect-ratio: 16/9;
    background: #000;
    border-radius: var(--pp-radius);
    overflow: hidden;
}
.pp-video-embed iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
}
.pp-video-placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    flex-direction: column;
    gap: 12px;
}
.pp-video-placeholder__icon {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--pp-teal);
    display: flex;
    align-items: center;
    justify-content: center;
}
.pp-video-placeholder__icon i {
    color: var(--pp-black);
    font-size: 1.25rem;
    margin-left: 3px;
}
.pp-podcast-mini {
    display: flex;
    gap: 12px;
    padding: 14px;
}
.pp-podcast-mini__thumb {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    background: var(--pp-teal-dim);
    border-radius: var(--pp-radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
}
.pp-podcast-mini__thumb i { color: var(--pp-red, #CC2222); }
.pp-podcast-mini__body { min-width: 0; }
.pp-podcast-mini__title {
    font-weight: 600;
    color: var(--pp-text);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    font-size: 0.875rem;
}

/* ═══════════════════════════════════════
   SUPPORT CTA
   ═══════════════════════════════════════ */
.pp-support-btn {
    border-color: rgba(247,147,26,0.4);
    color: #f7931a;
    margin-right: 8px;
}

/* ═══════════════════════════════════════
   STICKY SUPPORT BAR
   ═══════════════════════════════════════ */
.pp-support-bar {
    position: fixed;
    bottom: 0; left: 0; right: 0;
    z-index: 90;
    background: rgba(10,10,10,0.95);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-top: 1px solid rgba(245,166,35,0.15);
    padding: 10px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    transform: translateY(100%);
    transition: transform 0.4s cubic-bezier(0.4,0,0.2,1);
}
.pp-support-bar.is-visible { transform: translateY(0); }
.pp-support-bar__text {
    font-family: var(--pp-font-mono);
    font-size: 0.6875rem;
    color: rgba(255,255,255,0.45);
    letter-spacing: 0.04em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}
.pp-support-bar__actions { display: flex; gap: 8px; flex-shrink: 0; }
.pp-support-bar__btn {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 7px 14px;
    border-radius: 6px;
    font-family: var(--pp-font-mono);
    font-size: 0.625rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    transition: all 0.2s ease;
    white-space: nowrap;
}
.pp-support-bar__btn--lightning {
    background: rgba(245,166,35,0.1);
    border: 1px solid rgba(245,166,35,0.3);
    color: var(--pp-amber);
}
.pp-support-bar__btn--lightning:hover {
    background: rgba(245,166,35,0.2);
    border-color: rgba(245,166,35,0.5);
    color: var(--pp-amber);
}
.pp-support-bar__dismiss {
    background: none;
    border: none;
    color: rgba(255,255,255,0.2);
    cursor: pointer;
    padding: 6px;
    font-size: 0.75rem;
    flex-shrink: 0;
    transition: color 0.15s;
}
.pp-support-bar__dismiss:hover { color: rgba(255,255,255,0.5); }

/* ═══════════════════════════════════════
   ANIMATIONS
   ═══════════════════════════════════════ */
@keyframes pp-oracle-pulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.08); opacity: 1; }
}
@keyframes pp-pulse-dot-glow {
    0%, 100% { opacity: 0.5; box-shadow: 0 0 4px rgba(245,166,35,0.3); }
    50% { opacity: 1; box-shadow: 0 0 8px rgba(245,166,35,0.6); }
}

/* ═══════════════════════════════════════
   RESPONSIVE
   ═══════════════════════════════════════ */
@media (max-width: 767px) {
    .pp-hero { padding: 100px 0 60px; }
    .pp-hero__price-widget { flex-direction: column; align-items: flex-start; gap: 12px; }
    .pp-hero__price-value { font-size: 1.5rem; }
    .pp-support-bar__text { display: none; }
    .pp-oracle-card__inner { padding: 24px; gap: 20px; }
    .pp-oracle-card__avatar { width: 120px; height: 120px; }
}

/* ═══════════════════════════════════════
   LIGHT MODE OVERRIDES
   ═══════════════════════════════════════ */
[data-theme="light"] .pp-hero__title { color: #000; }
[data-theme="light"] .pp-hero__subtitle { color: #333; }
[data-theme="light"] .pp-hero__title-accent { color: #CC2222; }
[data-theme="light"] .pp-hero { background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%); }
</style>
{% endblock %}

{% block content %}

<!-- ═══════════════════════════════════════════════════════════════
     HERO SECTION
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-hero">
    <div class="pp-hero__orb pp-hero__orb--1"></div>
    <div class="pp-hero__orb pp-hero__orb--2"></div>
    <div class="pp-hero__orb pp-hero__orb--3"></div>

    <div class="pp-container">
        <div class="pp-hero__content">

            <!-- Live badge -->
            <div class="pp-hero__badge pp-animate-in">
                <span class="pp-pulse-dot"></span>
                LIVE INTELLIGENCE FEED
            </div>

            <!-- Title -->
            <h1 class="pp-hero__title pp-animate-in pp-animate-in-delay-1">
                Bitcoin Intelligence <br>
                <span class="pp-hero__title-accent">for Transactors</span>
            </h1>

            <!-- Subtitle -->
            <p class="pp-hero__subtitle pp-animate-in pp-animate-in-delay-2">
                Real-time market signals, investigative reporting, and deep-protocol analysis.
                Built for people who actually use Bitcoin.
            </p>

            <!-- CTAs -->
            <div class="pp-hero__actions pp-animate-in pp-animate-in-delay-3">
                <a href="/articles" class="pp-btn pp-btn--primary pp-btn--lg">
                    <i class="fas fa-terminal"></i> Read Intel Briefs
                </a>
                <a href="/bitfeed-live" class="pp-btn pp-btn--ghost pp-btn--lg">
                    <i class="fas fa-heartbeat"></i> Live Pulse
                </a>
                <a href="/stage" class="pp-btn pp-btn--ghost pp-btn--lg pp-hero__btn-stage">
                    <i class="fas fa-broadcast-tower"></i> Stage ⚡
                </a>
            </div>

            <!-- New here? -->
            <div class="pp-hero__new-here pp-animate-in pp-animate-in-delay-4">
                <span class="pp-hero__new-here-pill">New here?</span>
                <span class="pp-hero__new-here-text">
                    Start with <a href="/dossier" class="pp-hero__new-here-link">The Dossier</a> — 32 intelligence briefs on why Bitcoin wins.
                    <span class="pp-hero__new-here-sep">·</span>
                    <a href="/stage" class="pp-hero__new-here-link pp-hero__new-here-link--stage">Watch the Avatar Stage ⚡</a>
                </span>
            </div>

            <!-- BTC Price Widget (server-rendered, live-updated by JS) -->
            <div class="pp-hero__price-widget pp-animate-in pp-animate-in-delay-4">
                <div>
                    <div class="pp-hero__price-label">Bitcoin</div>
                    <div class="pp-hero__price-value" id="heroBtcPrice">
                        {% if prices and prices.get('bitcoin') %}
                            ${{ "{:,.0f}".format(prices['bitcoin'].get('usd', 0)) }}
                        {% else %}
                            $--,---
                        {% endif %}
                    </div>
                </div>
                <div id="heroBtcChange" class="pp-hero__price-change {% if prices and prices.get('bitcoin') and prices['bitcoin'].get('usd_24h_change', 0) >= 0 %}pp-hero__price-change--up{% else %}pp-hero__price-change--down{% endif %}">
                    {% if prices and prices.get('bitcoin') %}
                        {% set change = prices['bitcoin'].get('usd_24h_change', 0) %}
                        {{ "+%.1f"|format(change) if change >= 0 else "%.1f"|format(change) }}%
                    {% else %}
                        --%
                    {% endif %}
                </div>
                <canvas id="heroSparkline" class="pp-hero__price-chart" width="80" height="36"></canvas>
            </div>

        </div>
    </div>
    <div class="pp-hero__pulse-line"></div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     TODAY'S SIGNAL
     ═══════════════════════════════════════════════════════════════ -->
{% if todays_signal %}
<section class="pp-section" style="padding-top:32px;padding-bottom:0;">
    <div class="pp-container">
        <div class="pp-signal pp-reveal">
            <div class="pp-signal__icon">
                <i class="fas fa-satellite-dish"></i>
            </div>
            <div>
                <div class="pp-label" style="margin-bottom:4px;">TODAY'S SIGNAL</div>
                <p class="pp-signal__text">{{ todays_signal }}</p>
            </div>
        </div>
    </div>
</section>
{% endif %}

<!-- ═══════════════════════════════════════════════════════════════
     STATS COUNTERS — LIVE NETWORK DATA
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section">
    <div class="pp-container">
        <div class="pp-stats-row pp-reveal" id="statsRow">
            <div class="pp-stat pp-stat--loading" id="statHashrate">
                <div class="pp-stat__value pp-stat__value--live" id="statHashrateVal">---</div>
                <div class="pp-stat__label">EH/s Hashrate</div>
            </div>
            <div class="pp-stat pp-stat--loading" id="statDifficulty">
                <div class="pp-stat__value pp-stat__value--live" id="statDifficultyVal">---</div>
                <div class="pp-stat__label">T Difficulty</div>
            </div>
            <div class="pp-stat pp-stat--loading" id="statArticles">
                <div class="pp-stat__value pp-stat__value--live" id="statArticlesVal">
                    {{ (recent_articles|length + featured_articles|length) }}+
                </div>
                <div class="pp-stat__label">Intel Briefs</div>
            </div>
            <div class="pp-stat">
                <div class="pp-stat__value">24/7</div>
                <div class="pp-stat__label">Live Coverage</div>
            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     FEATURED ARTICLES
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section" style="padding-top:0;">
    <div class="pp-container">
        <div class="pp-section__title-bar pp-mb-4">
            <div>
                <h2 class="pp-section__title">Latest Intelligence</h2>
                <p class="pp-section__subtitle">Real-time analysis from the Protocol Pulse team</p>
            </div>
            <a href="/articles" class="pp-btn pp-btn--ghost pp-btn--sm">
                View All <i class="fas fa-arrow-right" style="margin-left:4px;font-size:0.7rem;"></i>
            </a>
        </div>

        {% if featured_articles and featured_articles|length > 0 %}
        <!-- Featured article (full width) -->
        <div class="pp-mb-4 pp-reveal">
            {% set fa = featured_articles[0] %}
            <article class="pp-card pp-card--featured">
                <img src="{{ article_image_urls.get(fa.id, '/static/images/default-header.png') }}"
                     alt="{{ fa.title }}"
                     class="pp-card__image"
                     loading="lazy"
                     decoding="async"
                     onerror="this.src='/static/images/default-header.png'">
                <div class="pp-card__body">
                    <span class="pp-badge pp-badge--teal pp-mb-1">{{ fa.category or 'Intelligence' }}</span>
                    <h3 class="pp-card__title" style="font-size:1.5rem;-webkit-line-clamp:3;">
                        <a href="/article/{{ fa.id }}">{{ fa.title }}</a>
                    </h3>
                    <p class="pp-card__excerpt" style="-webkit-line-clamp:4;">
                        {{ fa.summary or fa.content[:200]|striptags + '...' }}
                    </p>
                    <div class="pp-card__meta">
                        <span>{{ fa.published_at.strftime('%b %d, %Y') if fa.published_at else fa.created_at.strftime('%b %d, %Y') }}</span>
                        <span class="pp-card__meta-divider"></span>
                        <span>{{ ((fa.content|striptags|length) / 1200)|round|int }} min read</span>
                    </div>
                </div>
            </article>
        </div>
        {% endif %}

        <!-- Article grid -->
        <div class="pp-grid pp-grid-3 pp-stagger">
            {% for article in recent_articles[:6] %}
            <article class="pp-card">
                <img src="{{ article_image_urls.get(article.id, '/static/images/default-header.png') }}"
                     alt="{{ article.title }}"
                     class="pp-card__image"
                     loading="lazy"
                     decoding="async"
                     onerror="this.src='/static/images/default-header.png'">
                <div class="pp-card__body">
                    <span class="pp-badge pp-badge--ghost pp-mb-1">{{ article.category or 'Intel' }}</span>
                    <h3 class="pp-card__title">
                        <a href="/article/{{ article.id }}">{{ article.title }}</a>
                    </h3>
                    <p class="pp-card__excerpt">
                        {{ article.summary or article.content[:160]|striptags + '...' }}
                    </p>
                    <div class="pp-card__meta">
                        <span>{{ article.published_at.strftime('%b %d') if article.published_at else article.created_at.strftime('%b %d') }}</span>
                        <span class="pp-card__meta-divider"></span>
                        <span>{{ ((article.content|striptags|length) / 1200)|round|int }} min</span>
                    </div>
                </div>
            </article>
            {% endfor %}
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     THE ORACLE — AI INTELLIGENCE ANALYST
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section" style="padding-top:0;padding-bottom:48px;">
    <div class="pp-container">
        <div class="pp-oracle-card pp-reveal">
            <div class="pp-oracle-card__scanlines"></div>
            <div class="pp-oracle-card__inner">

                <!-- Avatar -->
                <div class="pp-oracle-card__avatar">
                    <img src="{{ url_for('static', filename='images/protocol-pulse-logo-transparent.png') }}"
                         alt="The Oracle"
                         loading="lazy">
                    <div class="pp-oracle-card__pulse-ring"></div>
                </div>

                <!-- Text -->
                <div class="pp-oracle-card__body">
                    <div class="pp-oracle-card__badge">
                        <span class="pp-oracle-card__badge-dot"></span>
                        AI INTELLIGENCE ANALYST
                    </div>
                    <h2 class="pp-oracle-card__title">
                        Consult <span class="pp-oracle-card__title-gradient">The Oracle</span>
                    </h2>
                    <p class="pp-oracle-card__desc">
                        Real-time Bitcoin intelligence from our AI analyst.
                        Ask anything — market state, mining economics, protocol analysis.
                        Lip-synced avatar delivers briefings in seconds.
                    </p>
                    <div class="pp-oracle-card__actions">
                        <a href="/stage" class="pp-btn pp-btn--primary pp-btn--lg pp-oracle-card__btn-primary">
                            <i class="fas fa-bolt"></i> Consult The Oracle
                        </a>
                        <a href="/oracle" class="pp-btn pp-btn--ghost pp-btn--lg pp-oracle-card__btn-ghost">
                            <i class="fas fa-microphone"></i> Ask a Question
                        </a>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     PULSE CHECK — LATEST VIDEO
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section pp-pulse-check-section">
    <div class="pp-container">
        <div class="pp-layout-sidebar pp-reveal">
            <div>
                <div class="pp-section__header">
                    <span class="pp-badge pp-badge--live pp-mb-2">Daily</span>
                    <h2 class="pp-section__title">Pulse Check</h2>
                    <p class="pp-section__subtitle">Daily AI-powered Bitcoin intelligence briefing</p>
                </div>
                <div class="pp-video-embed" id="pulseCheckEmbed">
                    <div class="pp-video-placeholder" id="videoPlaceholder" onclick="ppLoadVideo()">
                        <div class="pp-video-placeholder__icon">
                            <i class="fas fa-play"></i>
                        </div>
                        <div class="pp-label">Watch Latest Episode</div>
                    </div>
                </div>
            </div>
            <div>
                <h3 class="pp-h5 pp-mb-3">Recent Episodes</h3>
                {% if featured_podcasts %}
                    {% for podcast in featured_podcasts[:3] %}
                    <a href="/podcasts" class="pp-card pp-podcast-mini pp-mb-2">
                        <div class="pp-podcast-mini__thumb">
                            <i class="fas fa-microphone"></i>
                        </div>
                        <div class="pp-podcast-mini__body">
                            <div class="pp-podcast-mini__title">{{ podcast.title }}</div>
                            <div class="pp-tiny pp-text-muted">{{ podcast.published_date.strftime('%b %d') if podcast.published_date else '' }}</div>
                        </div>
                    </a>
                    {% endfor %}
                {% else %}
                    <div class="pp-card" style="padding:20px;text-align:center;">
                        <div class="pp-text-muted pp-small">Episodes coming soon</div>
                    </div>
                {% endif %}
                <a href="/podcasts" class="pp-btn pp-btn--outline-teal pp-btn--sm pp-btn--full pp-mt-2">
                    All Episodes <i class="fas fa-arrow-right" style="margin-left:4px;font-size:0.7rem;"></i>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     INTELLIGENCE TOOLS GRID
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section">
    <div class="pp-container">
        <div class="pp-section__header pp-text-center pp-reveal">
            <h2 class="pp-section__title">Intelligence Tools</h2>
            <p class="pp-section__subtitle">Real-time analysis and monitoring for serious transactors</p>
        </div>

        <div class="pp-grid pp-grid-3 pp-stagger">
            <a href="/live" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-atom" style="color:var(--pp-red,#CC2222);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Live Terminal</h3>
                <p class="pp-small pp-text-secondary">Real-time blockchain data visualization with network stats, mempool activity, and mining metrics.</p>
            </a>
            <a href="/whale-watcher" class="pp-card pp-card--stat pp-card--amber">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-water" style="color:var(--pp-amber);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Whale Watcher</h3>
                <p class="pp-small pp-text-secondary">Monitor large Bitcoin transactions in real-time. See what the big players are doing before the market reacts.</p>
            </a>
            <a href="/map" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-map-marked-alt" style="color:var(--pp-red,#CC2222);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Merchant Map</h3>
                <p class="pp-small pp-text-secondary">Discover Bitcoin-accepting businesses worldwide. Circular economy, visualized.</p>
            </a>
            <a href="/solo-slayers" class="pp-card pp-card--stat pp-card--amber">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-hammer" style="color:var(--pp-amber);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Solo Slayers</h3>
                <p class="pp-small pp-text-secondary">Lottery miners who beat astronomical odds. Solo block finds, verified on-chain.</p>
            </a>
            <a href="/value-stream" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-bolt" style="color:var(--pp-red,#CC2222);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Value Stream</h3>
                <p class="pp-small pp-text-secondary">Lightning Network value flows and micro-transaction intelligence. The speed layer, analyzed.</p>
            </a>
            <a href="/chat" class="pp-card pp-card--stat pp-card--amber">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-robot" style="color:var(--pp-amber);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Ask Alex</h3>
                <p class="pp-small pp-text-secondary">AI-powered Bitcoin analyst. Ask anything about the protocol, market dynamics, or mining economics.</p>
            </a>
            <a href="/nostr-signal" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-satellite-dish" style="color:var(--pp-amber);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Nostr Signal Radar</h3>
                <p class="pp-small pp-text-secondary">Real-time alpha from 50+ Bitcoin OGs on Nostr — confidence-scored before it trends anywhere else.</p>
            </a>
            <a href="/network-health" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-heartbeat" style="color:var(--pp-red,#CC2222);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Network Health</h3>
                <p class="pp-small pp-text-secondary">Real-time Bitcoin network vitals: mempool depth, fee rates, hashrate, difficulty, and block times.</p>
            </a>
            <a href="/charts" class="pp-card pp-card--stat pp-card--amber">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-chart-line" style="color:var(--pp-amber);"></i></div>
                <h3 class="pp-h5 pp-mb-1">BTC Charts</h3>
                <p class="pp-small pp-text-secondary">Interactive price, hashrate, and fee charts with technical overlays. Deep history, clean UI.</p>
            </a>
            <a href="/stage" class="pp-card pp-card--stat">
                <div style="font-size:1.5rem;margin-bottom:8px;"><i class="fas fa-broadcast-tower" style="color:var(--pp-red,#CC2222);"></i></div>
                <h3 class="pp-h5 pp-mb-1">Avatar Stage</h3>
                <p class="pp-small pp-text-secondary">24/7 Bitcoin intelligence from the AI avatar. Live sentiment, real-time transcript, partner tracking.</p>
            </a>
        </div>

        <!-- Support CTA -->
        <div class="pp-reveal" style="margin-top:32px;text-align:center;">
            <p class="pp-small pp-text-secondary" style="margin-bottom:12px;">
                Protocol Pulse is ad-free and sovereign. Support it with sats.
            </p>
            <a href="/donate/bitcoin" class="pp-btn pp-btn--ghost pp-btn--sm pp-support-btn">
                <i class="fas fa-bolt"></i> Support with Bitcoin
            </a>
            <a href="/dossier" class="pp-btn pp-btn--ghost pp-btn--sm">
                <i class="fas fa-folder-open"></i> The Dossier
            </a>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════════
     NEWSLETTER CTA
     ═══════════════════════════════════════════════════════════════ -->
<section class="pp-section">
    <div class="pp-container">
        <div class="pp-newsletter pp-reveal">
            <h2 class="pp-newsletter__title">Stay on the Signal</h2>
            <p class="pp-newsletter__subtitle">Daily Bitcoin intelligence delivered to your inbox. No spam, no shilling — just signal.</p>
            <form class="pp-newsletter__form" action="/newsletter/subscribe" method="POST">
                <input type="email" name="email" class="pp-input" placeholder="your@email.com" required aria-label="Email address">
                <button type="submit" class="pp-btn pp-btn--primary">Subscribe</button>
            </form>
            <p class="pp-tiny pp-text-muted pp-mt-2">Join thousands of transactors. Unsubscribe anytime.</p>
        </div>
    </div>
</section>

{% endblock %}

{% block scripts %}
<script>
(function() {
    'use strict';

    /* ── LIVE NETWORK STATS ──────────────────────────────────────────
       Fetches /api/network-stats which is live on this server.
       Falls back gracefully — never shows broken/empty data.
    ──────────────────────────────────────────────────────────────── */
    function loadNetworkStats() {
        fetch('/api/network-stats', { signal: AbortSignal.timeout(6000) })
            .then(function(r) { return r.ok ? r.json() : Promise.reject(r.status); })
            .then(function(data) {
                if (data && data.success !== false) {
                    // Hashrate
                    var hrEl = document.getElementById('statHashrateVal');
                    if (hrEl && data.hashrate) {
                        hrEl.textContent = data.hashrate;
                        hrEl.closest('.pp-stat').classList.remove('pp-stat--loading');
                    }
                    // Difficulty (strip 'T' if present, add it back cleanly)
                    var difEl = document.getElementById('statDifficultyVal');
                    if (difEl && data.difficulty_progress) {
                        var raw = String(data.difficulty_progress).replace(/[^\d.]/g,'');
                        difEl.textContent = parseFloat(raw).toFixed(0) + 'T';
                        difEl.closest('.pp-stat').classList.remove('pp-stat--loading');
                    }
                }
            })
            .catch(function() { /* Silently keep server-rendered fallbacks */ });
    }

    /* ── LIVE ARTICLE COUNT ──────────────────────────────────────────
       Hits /api/latest-articles which returns article data.
       Updates the stat counter with real published count.
    ──────────────────────────────────────────────────────────────── */
    function loadArticleCount() {
        fetch('/api/latest-articles', { signal: AbortSignal.timeout(5000) })
            .then(function(r) { return r.ok ? r.json() : Promise.reject(r.status); })
            .then(function(data) {
                var countEl = document.getElementById('statArticlesVal');
                if (!countEl) return;
                var count = Array.isArray(data) ? data.length : (data.count || data.total || null);
                if (count) {
                    // Show real total — if it's just page results, show "X+" to indicate more
                    countEl.textContent = count >= 100 ? count.toLocaleString() : count + '+';
                    countEl.closest('.pp-stat').classList.remove('pp-stat--loading');
                }
            })
            .catch(function() { /* Keep Jinja2 server-rendered fallback */ });
    }

    /* ── HERO BTC PRICE — LIVE UPDATE ───────────────────────────────
       Server renders initial price. JS updates every 60s.
       Single setInterval, stored for proper cleanup.
    ──────────────────────────────────────────────────────────────── */
    var _priceInterval = null;

    function updateHeroPrice() {
        fetch('https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd&include_24hr_change=true',
              { signal: AbortSignal.timeout(8000) })
            .then(function(r) { return r.ok ? r.json() : Promise.reject(r.status); })
            .then(function(data) {
                if (!data.bitcoin) return;
                var el = document.getElementById('heroBtcPrice');
                var ch = document.getElementById('heroBtcChange');
                if (el) el.textContent = '$' + data.bitcoin.usd.toLocaleString('en-US', {maximumFractionDigits: 0});
                if (ch) {
                    var pct = data.bitcoin.usd_24h_change;
                    ch.textContent = (pct >= 0 ? '+' : '') + pct.toFixed(1) + '%';
                    ch.className = 'pp-hero__price-change pp-hero__price-change--' + (pct >= 0 ? 'up' : 'down');
                }
            })
            .catch(function() { /* Retain server-rendered price on failure */ });
    }

    /* Start price polling — first update at 60s, then every 60s.
       Store ref so we can clear on page unload (prevents memory leak). */
    var _priceInitTimer = setTimeout(function() {
        updateHeroPrice();
        _priceInterval = setInterval(updateHeroPrice, 60000);
    }, 60000);

    /* Cleanup on page unload */
    window.addEventListener('beforeunload', function() {
        clearTimeout(_priceInitTimer);
        if (_priceInterval) clearInterval(_priceInterval);
    });

    /* ── MINI SPARKLINE ─────────────────────────────────────────────
       24h hourly price chart drawn on canvas.
    ──────────────────────────────────────────────────────────────── */
    function drawSparkline() {
        var canvas = document.getElementById('heroSparkline');
        if (!canvas || !canvas.getContext) return;
        var ctx = canvas.getContext('2d');
        var w = canvas.width, h = canvas.height;

        fetch('https://api.coingecko.com/api/v3/coins/bitcoin/market_chart?vs_currency=usd&days=1&interval=hourly',
              { signal: AbortSignal.timeout(10000) })
            .then(function(r) { return r.ok ? r.json() : Promise.reject(r.status); })
            .then(function(data) {
                if (!data.prices || data.prices.length < 2) return;
                var prices = data.prices.map(function(p) { return p[1]; });
                var min = Math.min.apply(null, prices);
                var max = Math.max.apply(null, prices);
                var range = max - min || 1;
                var isUp = prices[prices.length - 1] >= prices[0];

                ctx.beginPath();
                ctx.strokeStyle = isUp ? '#00d4aa' : '#ff4757';
                ctx.lineWidth = 1.5;
                ctx.lineJoin = 'round';

                for (var i = 0; i < prices.length; i++) {
                    var x = (i / (prices.length - 1)) * w;
                    var y = h - ((prices[i] - min) / range) * (h - 4) - 2;
                    if (i === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
                }
                ctx.stroke();
            })
            .catch(function() { /* Sparkline fails silently */ });
    }

    /* ── PULSE CHECK VIDEO ──────────────────────────────────────────
       Shows latest Pulse Check from the video server.
       Falls back to /podcasts link if video not available.
    ──────────────────────────────────────────────────────────────── */
    window.ppLoadVideo = function() {
        var container = document.getElementById('pulseCheckEmbed');
        if (!container) return;

        /* Latest render from the video server */
        var videoUrl = 'https://video.protocolpulse.io/video_pipeline_v3/output/pulse_check_20260308_123752.mp4';

        container.innerHTML =
            '<video controls autoplay style="position:absolute;inset:0;width:100%;height:100%;background:#000;" ' +
            'poster="/static/images/protocol-pulse-logo-transparent.png">' +
            '<source src="' + videoUrl + '" type="video/mp4">' +
            '<p style="color:rgba(255,255,255,0.5);padding:20px;">Video unavailable — ' +
            '<a href="/podcasts" style="color:var(--pp-teal);">visit Podcasts</a></p>' +
            '</video>';
    };

    /* ── SCROLL REVEAL ──────────────────────────────────────────────
       IntersectionObserver for .pp-reveal elements.
    ──────────────────────────────────────────────────────────────── */
    if ('IntersectionObserver' in window) {
        var revealObserver = new IntersectionObserver(function(entries) {
            entries.forEach(function(entry) {
                if (entry.isIntersecting) {
                    entry.target.classList.add('pp-visible');
                    revealObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

        document.querySelectorAll('.pp-reveal:not(.pp-visible)').forEach(function(el) {
            revealObserver.observe(el);
        });
    }

    /* ── COUNTER ANIMATION ──────────────────────────────────────────
       Animates [data-count] elements when they scroll into view.
    ──────────────────────────────────────────────────────────────── */
    if ('IntersectionObserver' in window) {
        var animated = new Set();
        var counterObserver = new IntersectionObserver(function(entries) {
            entries.forEach(function(entry) {
                if (!entry.isIntersecting) return;
                var el = entry.target;
                if (animated.has(el)) return;
                animated.add(el);
                var target = parseInt(el.getAttribute('data-count'));
                if (!target || isNaN(target)) return;
                var duration = 1500, startTime = null;
                function step(ts) {
                    if (!startTime) startTime = ts;
                    var progress = Math.min((ts - startTime) / duration, 1);
                    var eased = 1 - Math.pow(1 - progress, 3);
                    el.textContent = Math.floor(eased * target).toLocaleString();
                    if (progress < 1) requestAnimationFrame(step);
                }
                requestAnimationFrame(step);
            });
        }, { threshold: 0.3 });

        document.querySelectorAll('[data-count]').forEach(function(el) {
            counterObserver.observe(el);
        });
    }

    /* ── BOOT SEQUENCE ──────────────────────────────────────────── */
    loadNetworkStats();
    loadArticleCount();
    drawSparkline();

})();
</script>
{% endblock %}
