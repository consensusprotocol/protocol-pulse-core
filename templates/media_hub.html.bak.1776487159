{% extends "base.html" %}
{% block title %}Bitcoin Media Command Center — Protocol Pulse<section id="books" style="background:#0a0a0a;border-top:1px solid rgba(220,38,38,0.15);padding:60px 0;"><div style="max-width:1200px;margin:0 auto;padding:0 24px;"><div style="display:flex;align-items:center;gap:12px;margin-bottom:40px;"><div style="width:3px;height:32px;background:#dc2626;border-radius:2px;"></div><div><div style="font-family:JetBrains Mono,monospace;font-size:0.62rem;letter-spacing:0.15em;color:#dc2626;text-transform:uppercase;margin-bottom:4px;">Sovereign Library</div><h2 style="font-family:JetBrains Mono,monospace;font-size:1.5rem;font-weight:800;color:#fff;margin:0;">Book Series</h2></div></div>{% if our_books %}<div style="margin-bottom:48px;"><div style="font-family:JetBrains Mono,monospace;font-size:0.6rem;letter-spacing:0.12em;color:rgba(255,255,255,0.35);text-transform:uppercase;margin-bottom:20px;">Protocol Pulse Original Series</div><div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:24px;">{% for book in our_books %}<a href="{{ book.amazon_url }}" target="_blank" rel="noopener" style="text-decoration:none;display:block;background:rgba(255,255,255,0.03);border:1px solid rgba(220,38,38,0.2);border-radius:8px;overflow:hidden;transition:transform 0.2s,border-color 0.2s;" onmouseover="this.style.transform='translateY(-4px)';this.style.borderColor='rgba(220,38,38,0.5)'" onmouseout="this.style.transform='';this.style.borderColor='rgba(220,38,38,0.2)'"><div style="aspect-ratio:3/4;overflow:hidden;background:#111;"><img src="{{ book.cover_url }}" alt="{{ book.title }}" style="width:100%;height:100%;object-fit:cover;" loading="lazy"></div><div style="padding:12px;"><div style="font-family:JetBrains Mono,monospace;font-size:0.72rem;font-weight:700;color:#fff;margin-bottom:4px;line-height:1.3;">{{ book.title }}</div><div style="font-size:0.65rem;color:rgba(255,255,255,0.4);margin-bottom:8px;">{{ book.author }}</div><div style="font-family:JetBrains Mono,monospace;font-size:0.58rem;color:#dc2626;letter-spacing:0.08em;">GET ON AMAZON &#8594;</div></div></a>{% endfor %}</div></div>{% endif %}{% if recommended_books %}<div><div style="font-family:JetBrains Mono,monospace;font-size:0.6rem;letter-spacing:0.12em;color:rgba(255,255,255,0.35);text-transform:uppercase;margin-bottom:20px;">Essential Bitcoin Reading</div><div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:20px;">{% for book in recommended_books %}<a href="{{ book.amazon_url }}" target="_blank" rel="noopener" style="text-decoration:none;display:block;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.08);border-radius:8px;overflow:hidden;transition:transform 0.2s;" onmouseover="this.style.transform='translateY(-4px)'" onmouseout="this.style.transform=''"><div style="aspect-ratio:3/4;overflow:hidden;background:#111;"><img src="{{ book.cover_url }}" alt="{{ book.title }}" style="width:100%;height:100%;object-fit:cover;" loading="lazy"></div><div style="padding:10px;"><div style="font-family:JetBrains Mono,monospace;font-size:0.65rem;font-weight:700;color:#fff;margin-bottom:3px;line-height:1.3;">{{ book.title }}</div><div style="font-size:0.6rem;color:rgba(255,255,255,0.35);">{{ book.author }}</div>{% if book.bestseller %}<div style="margin-top:6px;display:inline-block;padding:2px 6px;background:rgba(247,147,26,0.15);border:1px solid rgba(247,147,26,0.3);border-radius:3px;font-size:0.52rem;color:#f7931a;font-family:JetBrains Mono,monospace;">ESSENTIAL</div>{% endif %}</div></a>{% endfor %}</div></div>{% endif %}</div></section>{% endblock %}
{% block meta_description %}The definitive Bitcoin media hub. 20+ voices. Live feeds from top podcasts, YouTube channels, and KOL intelligence. One screen.{% endblock %}
{% block og_meta %}
<meta property="og:title" content="Bitcoin Media Command Center — Protocol Pulse">
<meta property="og:description" content="The definitive Bitcoin media hub. 20+ voices. Live feeds from top podcasts, YouTube channels, and KOL intelligence.">
<meta property="og:type" content="website">
<meta property="og:site_name" content="Protocol Pulse">
<meta property="og:image" content="{{ url_for('static', filename='images/protocol-pulse-logo-transparent.png', _external=True) }}">
<meta property="og:url" content="{{ request.url }}">
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:site" content="@ProtocolPulse">
{% endblock %}
{% block head %}
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
  --void:#000;--deep:#040408;--card:#08080e;--elevated:#0e0e16;--hover:#14141e;
  --border:rgba(255,255,255,0.05);--border-h:rgba(255,255,255,0.1);
  --bright:#f5f5f5;--pri:#e0e0e0;--sec:rgba(255,255,255,0.5);--mut:rgba(255,255,255,0.25);
  --red:#dc2626;--red-g:rgba(220,38,38,0.12);--btc:#f7931a;
  --gold:#f8c15c;--cyan:#5de4ff;--lime:#89ffb8;--coral:#ff8ba0;
}
*{box-sizing:border-box;margin:0;padding:0}
.mh{font-family:'DM Sans',-apple-system,sans-serif;background:var(--void);color:var(--pri);min-height:100vh;padding-top:80px}
.mono{font-family:'JetBrains Mono',monospace}

/* ── TICKER HERO ── */
.ticker-hero{position:relative;overflow:hidden;background:linear-gradient(180deg,rgba(220,38,38,0.04),var(--void) 60%);border-bottom:1px solid var(--border)}
.ticker-wrap{position:relative;overflow:hidden;background:linear-gradient(90deg,rgba(220,38,38,0.06),rgba(8,8,14,0.95),rgba(220,38,38,0.06));border-bottom:1px solid var(--border);height:38px}
.ticker-track{display:flex;animation:tickerScroll 120s linear infinite;white-space:nowrap;height:100%;align-items:center;will-change:transform}
.ticker-track:hover{animation-play-state:paused}
.ticker-item{display:inline-flex;align-items:center;gap:6px;padding:0 24px;font-size:12px;color:var(--sec);text-decoration:none;transition:color .2s;flex-shrink:0;height:100%}
.ticker-item:hover{color:var(--bright)}
.ticker-src{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--red);text-transform:uppercase;letter-spacing:1px;font-weight:600}
.ticker-title{max-width:280px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.ticker-score{font-family:'JetBrains Mono',monospace;font-size:9px;padding:2px 6px;border-radius:10px;font-weight:600}
.ticker-score.high{background:rgba(137,255,184,0.1);color:var(--lime)}
.ticker-score.mid{background:rgba(248,193,92,0.1);color:var(--gold)}
.ticker-score.low{background:rgba(255,255,255,0.05);color:var(--mut)}
.ticker-sep{width:1px;height:14px;background:var(--border);flex-shrink:0;margin:0 4px}
.ticker-time{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut)}
@keyframes tickerScroll{from{transform:translateX(0)}to{transform:translateX(-50%)}}

.hero-content{max-width:1440px;margin:0 auto;padding:48px clamp(16px,4vw,48px) 40px;position:relative}
.hero-bg{position:absolute;inset:0;overflow:hidden;pointer-events:none}
.hero-orb{position:absolute;border-radius:50%;filter:blur(80px);animation:orbFloat 8s ease-in-out infinite}
.hero-orb-1{width:400px;height:400px;background:rgba(220,38,38,0.08);top:-100px;left:20%}
.hero-orb-2{width:300px;height:300px;background:rgba(248,193,92,0.05);bottom:-80px;right:15%;animation-delay:-3s}
@keyframes orbFloat{0%,100%{transform:translate(0,0) scale(1)}50%{transform:translate(30px,-20px) scale(1.1)}}
.hero-tag{display:inline-flex;align-items:center;gap:8px;padding:6px 14px;background:rgba(220,38,38,0.08);border:1px solid rgba(220,38,38,0.15);border-radius:24px;margin-bottom:20px;opacity:0;animation:fadeUp .6s ease forwards}
.hero-tag-dot{width:6px;height:6px;border-radius:50%;background:var(--red);animation:tagPulse 2s infinite}
@keyframes tagPulse{0%,100%{opacity:1}50%{opacity:0.3}}
.hero-tag-text{font-family:'JetBrains Mono',monospace;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--red)}
.hero-h{font-size:clamp(32px,6vw,56px);font-weight:700;line-height:1.05;color:var(--bright);letter-spacing:-2px;margin-bottom:12px;opacity:0;animation:fadeUp .6s ease .1s forwards}
.hero-h em{font-style:normal;background:linear-gradient(135deg,var(--red),var(--btc));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.hero-sub{font-size:14px;color:var(--sec);max-width:520px;line-height:1.6;margin-bottom:28px;opacity:0;animation:fadeUp .6s ease .2s forwards}
.hero-stats{display:flex;gap:28px;flex-wrap:wrap;opacity:0;animation:fadeUp .6s ease .3s forwards}
.hero-stat{position:relative;padding:8px 0}
.hero-stat::after{content:'';position:absolute;right:-14px;top:50%;transform:translateY(-50%);width:1px;height:18px;background:var(--border)}
.hero-stat:last-child::after{display:none}
.hero-stat-val{font-family:'JetBrains Mono',monospace;font-size:22px;font-weight:700;color:var(--bright)}
.hero-stat-lab{font-size:9px;color:var(--mut);text-transform:uppercase;letter-spacing:1.5px;margin-top:2px}
@keyframes fadeUp{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:translateY(0)}}

/* ── WRAP ── */
.wrap{max-width:1440px;margin:0 auto;padding:0 clamp(16px,4vw,48px)}

/* ── FEED MATRIX ── */
.matrix{display:grid;grid-template-columns:1fr 1fr 1fr;gap:20px;padding:48px 0}
.matrix-col{min-width:0}
.matrix-head{display:flex;align-items:center;gap:10px;margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--border)}
.matrix-head-icon{width:28px;height:28px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:12px;flex-shrink:0}
.matrix-head-icon.pod{background:rgba(220,38,38,0.1);color:var(--red);border:1px solid rgba(220,38,38,0.2)}
.matrix-head-icon.vid{background:rgba(248,193,92,0.1);color:var(--gold);border:1px solid rgba(248,193,92,0.2)}
.matrix-head-icon.kol{background:rgba(93,228,255,0.1);color:var(--cyan);border:1px solid rgba(93,228,255,0.2)}
.matrix-head-title{font-family:'JetBrains Mono',monospace;font-size:11px;letter-spacing:2px;text-transform:uppercase;font-weight:700;color:var(--bright)}
.matrix-head-count{font-family:'JetBrains Mono',monospace;font-size:10px;color:var(--mut);margin-left:auto}
.matrix-feed{max-height:calc(100vh - 200px);overflow-y:auto;padding-right:4px}
.matrix-feed::-webkit-scrollbar{width:3px}
.matrix-feed::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.06);border-radius:2px}

/* ── FEED ITEM ── */
.fi{display:block;padding:14px;margin-bottom:8px;border-radius:10px;background:rgba(255,255,255,0.015);border:1px solid rgba(255,255,255,0.04);transition:all .25s;text-decoration:none;color:inherit;position:relative}
.fi:hover{border-color:rgba(220,38,38,0.2);background:rgba(255,255,255,0.03);transform:translateY(-2px);box-shadow:0 8px 24px rgba(0,0,0,.3)}
.fi-row{display:flex;gap:12px;align-items:flex-start}
.fi-thumb{width:52px;height:52px;border-radius:8px;object-fit:cover;flex-shrink:0;background:var(--elevated)}
.fi-thumb-yt{width:80px;height:45px;border-radius:6px;object-fit:cover;flex-shrink:0;background:var(--elevated);position:relative;overflow:hidden}
.fi-play-ov{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.4);opacity:0;transition:opacity .2s}
.fi:hover .fi-play-ov{opacity:1}
.fi-play-circle{width:24px;height:24px;border-radius:50%;background:var(--red);display:flex;align-items:center;justify-content:center}
.fi-play-circle i{color:white;font-size:9px;margin-left:1px}
.fi-info{flex:1;min-width:0}
.fi-show{font-family:'JetBrains Mono',monospace;font-size:9px;text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:3px}
.fi-show.pod{color:var(--red)}.fi-show.vid{color:var(--gold)}.fi-show.kol{color:var(--cyan)}
.fi-title{font-size:13px;font-weight:600;color:var(--bright);line-height:1.35;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.fi-meta{display:flex;align-items:center;gap:8px;margin-top:6px;flex-wrap:wrap}
.fi-time{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut)}
.fi-dur{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut)}
.fi-score{font-family:'JetBrains Mono',monospace;font-size:9px;padding:2px 6px;border-radius:10px;font-weight:600}
.fi-score.high{background:rgba(137,255,184,0.1);color:var(--lime)}
.fi-score.mid{background:rgba(248,193,92,0.1);color:var(--gold)}
.fi-score.low{background:rgba(255,255,255,0.05);color:var(--mut)}
.fi-summary{font-size:11px;color:var(--sec);line-height:1.4;margin-top:6px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.fi-dir{font-family:'JetBrains Mono',monospace;font-size:8px;letter-spacing:1px;text-transform:uppercase;padding:2px 8px;border-radius:4px;font-weight:700}
.fi-dir.bullish{background:rgba(137,255,184,0.1);color:var(--lime)}
.fi-dir.bearish{background:rgba(255,139,160,0.1);color:var(--coral)}
.fi-dir.neutral{background:rgba(248,193,92,0.1);color:var(--gold)}
.fi-wave{display:flex;align-items:end;gap:1px;height:18px;margin-top:8px;opacity:0.5}
.fi:hover .fi-wave{opacity:1}
.fi-wave-bar{width:2px;border-radius:1px;background:linear-gradient(to top,var(--red),rgba(220,38,38,0.3))}
.fi-new{position:absolute;top:8px;right:8px;display:flex;align-items:center;gap:3px;font-family:'JetBrains Mono',monospace;font-size:7px;letter-spacing:1px;text-transform:uppercase;color:var(--red);font-weight:700}
.fi-new-dot{width:5px;height:5px;border-radius:50%;background:var(--red);animation:tagPulse 1.5s infinite}
.fi-empty{text-align:center;padding:48px 16px;color:var(--mut);font-size:12px}

/* ── SKELETON ── */
.skel{background:linear-gradient(90deg,rgba(255,255,255,0.03) 25%,rgba(255,255,255,0.06) 50%,rgba(255,255,255,0.03) 75%);background-size:200% 100%;animation:skelShimmer 1.5s infinite;border-radius:6px}
@keyframes skelShimmer{from{background-position:200% 0}to{background-position:-200% 0}}

/* ── AUDIO BAR ── */
.abar{position:fixed;bottom:0;left:0;right:0;background:rgba(8,8,14,.95);border-top:1px solid var(--border);padding:10px 24px;display:none;align-items:center;gap:14px;z-index:1000;backdrop-filter:blur(20px)}
.abar.active{display:flex}
.abar-btn{background:none;border:none;color:var(--sec);cursor:pointer;padding:4px 8px;font-size:16px}
.abar-btn:hover{color:var(--bright)}
.abar-now{flex:1;min-width:0}
.abar-title{font-size:12px;color:var(--pri);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.abar-show{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--red);text-transform:uppercase;letter-spacing:1px}
.abar-progress{height:2px;background:var(--border);border-radius:1px;margin-top:4px;cursor:pointer}
.abar-progress-fill{height:100%;background:var(--red);border-radius:1px;width:0;transition:width .3s}
.abar-close{background:none;border:none;color:var(--mut);cursor:pointer;font-size:12px;padding:4px 8px}
.abar-close:hover{color:var(--bright)}

/* ── VIDEO MODAL ── */
.vm-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.85);z-index:2000;display:none;align-items:center;justify-content:center;backdrop-filter:blur(8px);opacity:0;transition:opacity .3s ease}
.vm-overlay.active{display:flex;opacity:1}
.vm-wrap{position:relative;width:90vw;max-width:960px;aspect-ratio:16/9;background:#000;border-radius:12px;overflow:hidden;border:1px solid var(--border);box-shadow:0 32px 96px rgba(0,0,0,.8)}
.vm-close{position:absolute;top:-40px;right:0;background:rgba(255,255,255,0.1);border:1px solid rgba(255,255,255,0.15);color:var(--bright);width:32px;height:32px;border-radius:50%;cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:14px;z-index:2001;transition:all .2s}
.vm-close:hover{background:var(--red);border-color:var(--red)}
.vm-wrap iframe{width:100%;height:100%;border:none}

/* ── NEWSLETTER CTA ── */
.nl{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:48px;text-align:center;position:relative;overflow:hidden;margin-bottom:48px}
.nl::before{content:'';position:absolute;inset:0;background:radial-gradient(ellipse at center,var(--red-g) 0%,transparent 60%);pointer-events:none}
.nl-h{font-size:24px;font-weight:700;color:var(--bright);margin-bottom:8px;position:relative}
.nl-p{font-size:13px;color:var(--sec);margin-bottom:24px;position:relative}
.nl-f{display:flex;gap:10px;max-width:400px;margin:0 auto;position:relative}
.nl-i{flex:1;padding:12px 16px;background:var(--void);border:1px solid var(--border);border-radius:8px;color:var(--bright);font-size:13px;outline:none}
.nl-i:focus{border-color:var(--red)}
.nl-i::placeholder{color:var(--mut)}
.nl-b{padding:12px 24px;background:var(--red);border:none;border-radius:8px;color:white;font-size:13px;font-weight:600;cursor:pointer;white-space:nowrap}

/* ── LOADER ── */
.kol-loader{display:inline-block;width:14px;height:14px;border:2px solid var(--border);border-top:2px solid var(--red);border-radius:50%;animation:spin .7s linear infinite;margin-top:10px}
@keyframes spin{to{transform:rotate(360deg)}}

/* ── SECTION HEADERS ── */
.sec-head{display:flex;align-items:center;gap:12px;margin-bottom:20px;padding-bottom:14px;border-bottom:1px solid var(--border)}
.sec-head-icon{width:32px;height:32px;border-radius:8px;display:flex;align-items:center;justify-content:center;font-size:13px;flex-shrink:0}
.sec-head-icon.net{background:rgba(220,38,38,0.1);color:var(--red);border:1px solid rgba(220,38,38,0.2)}
.sec-head-icon.vid{background:rgba(248,193,92,0.1);color:var(--gold);border:1px solid rgba(248,193,92,0.2)}
.sec-head-icon.sig{background:rgba(220,38,38,0.15);color:#ff3333;border:1px solid rgba(220,38,38,0.3)}
.sec-head-title{font-family:'JetBrains Mono',monospace;font-size:12px;letter-spacing:2px;text-transform:uppercase;font-weight:700;color:var(--bright)}
.sec-head-updated{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut);margin-left:auto}

/* ── NETWORK FEED GRID ── */
.feed-duo{padding:40px 0 0}
.feed-section{min-width:0}
.feed-list{display:flex;flex-direction:column;gap:8px;max-height:520px;overflow-y:auto;padding-right:4px}
.feed-list::-webkit-scrollbar{width:3px}
.feed-list::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.06);border-radius:2px}

/* ── NETWORK FEED ITEM ── */
.nfi{display:flex;gap:12px;padding:12px 14px;border-radius:8px;background:rgba(255,255,255,0.015);border:1px solid rgba(255,255,255,0.04);transition:all .2s}
.nfi:hover{border-color:rgba(220,38,38,0.2);background:rgba(255,255,255,0.03)}
.nfi-icon{width:28px;height:28px;border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:11px;flex-shrink:0;background:rgba(255,255,255,0.04);color:var(--sec)}
.nfi-body{flex:1;min-width:0}
.nfi-source{font-family:'JetBrains Mono',monospace;font-size:9px;text-transform:uppercase;letter-spacing:1px;font-weight:600;color:var(--red);margin-bottom:2px}
.nfi-text{font-size:12px;color:var(--pri);line-height:1.4;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}
.nfi-time{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut);margin-top:4px}
.nfi-sent{display:inline-block;width:6px;height:6px;border-radius:50%;margin-right:4px;vertical-align:middle}
.nfi-sent.bullish{background:#ff0000}.nfi-sent.bearish{background:#333}.nfi-sent.neutral{background:#666}

/* ── VIDEO INTEL SERIES CARDS ── */
.vi-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:14px}
.vi-card{background:var(--card);border:1px solid var(--border);border-radius:10px;overflow:hidden;cursor:pointer;transition:all .3s;text-decoration:none;color:inherit}
.vi-card:hover{border-color:rgba(220,38,38,0.4);transform:translateY(-3px);box-shadow:0 12px 40px rgba(220,38,38,0.08)}
.vi-card:hover .vi-img{transform:scale(1.05)}
.vi-img-w{position:relative;overflow:hidden;aspect-ratio:16/9;background:var(--elevated)}
.vi-img{width:100%;height:100%;object-fit:cover;transition:transform .5s}
.vi-ov{position:absolute;inset:0;background:linear-gradient(180deg,transparent 40%,rgba(0,0,0,.85) 100%)}
.vi-play{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);width:40px;height:40px;background:rgba(220,38,38,.85);border-radius:50%;display:flex;align-items:center;justify-content:center;opacity:0;transition:opacity .3s}
.vi-card:hover .vi-play{opacity:1}
.vi-play i{color:white;font-size:13px;margin-left:2px}
.vi-dur{position:absolute;top:8px;right:8px;background:rgba(0,0,0,.75);backdrop-filter:blur(6px);padding:2px 7px;border-radius:4px;font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--pri);border:1px solid rgba(255,255,255,.08)}
.vi-score{position:absolute;bottom:8px;left:8px;background:rgba(220,38,38,.85);padding:2px 8px;border-radius:10px;font-family:'JetBrains Mono',monospace;font-size:9px;color:white;font-weight:700}
.vi-new{position:absolute;top:8px;left:8px;background:var(--red);padding:2px 7px;border-radius:4px;font-family:'JetBrains Mono',monospace;font-size:8px;letter-spacing:1px;text-transform:uppercase;color:white;font-weight:700;animation:tagPulse 2s infinite}
.vi-body{padding:12px 14px 14px}
.vi-src{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--gold);text-transform:uppercase;letter-spacing:1px;font-weight:600;margin-bottom:4px}
.vi-title{font-size:13px;font-weight:700;color:var(--bright);line-height:1.3;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}

/* ── LIVE SIGNAL TERMINAL ── */
.signal-terminal{margin:40px 0 0;background:#0a0a0a;border:1px solid rgba(220,38,38,0.15);border-radius:12px;overflow:hidden}
.signal-terminal-head{display:flex;align-items:center;gap:12px;padding:14px 20px;border-bottom:1px solid rgba(255,255,255,0.05);background:rgba(220,38,38,0.03)}
.signal-terminal-dots{display:flex;gap:5px}
.signal-terminal-dots span{width:8px;height:8px;border-radius:50%}
.signal-terminal-dots span:nth-child(1){background:#ff3333;animation:tagPulse 2s infinite}
.signal-terminal-dots span:nth-child(2){background:#333}
.signal-terminal-dots span:nth-child(3){background:#333}
.signal-terminal-label{font-family:'JetBrains Mono',monospace;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--red);font-weight:700}
.signal-terminal-ts{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut);margin-left:auto}
.signal-feed{max-height:480px;overflow-y:auto;padding:8px 0}
.signal-feed::-webkit-scrollbar{width:3px}
.signal-feed::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.06);border-radius:2px}

/* ── SIGNAL ITEM ── */
.sig-item{display:flex;gap:12px;padding:10px 20px;border-left:3px solid #666;transition:all .2s;animation:sigFadeIn .4s ease}
.sig-item:hover{background:rgba(255,255,255,0.02)}
.sig-item.bullish{border-left-color:#ff0000}
.sig-item.bearish{border-left-color:#333}
.sig-item.neutral{border-left-color:#666}
.sig-time{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--mut);min-width:52px;flex-shrink:0;padding-top:2px}
.sig-src{font-family:'JetBrains Mono',monospace;font-size:9px;letter-spacing:1px;text-transform:uppercase;font-weight:700;min-width:80px;flex-shrink:0;padding-top:2px}
.sig-src.bullish{color:#ff0000}.sig-src.bearish{color:#555}.sig-src.neutral{color:#888}
.sig-text{font-size:12px;color:var(--pri);line-height:1.4;flex:1;min-width:0}
.sig-strength{display:flex;gap:2px;align-items:center;flex-shrink:0;padding-top:2px}
.sig-strength-bar{width:3px;height:10px;border-radius:1px;background:rgba(255,255,255,0.1)}
.sig-strength-bar.active{background:var(--red)}
@keyframes sigFadeIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}

/* ── RESPONSIVE ── */
@media(max-width:1024px){
  .matrix{grid-template-columns:1fr 1fr;gap:16px}
  .matrix-col.kol-col{grid-column:span 2}
  .vi-grid{grid-template-columns:repeat(auto-fill,minmax(180px,1fr))}
}
@media(max-width:768px){
  .matrix{grid-template-columns:1fr;gap:16px}
  .matrix-col.kol-col{grid-column:auto}
  .hero-content{padding:32px clamp(16px,4vw,48px) 28px}
  .ticker-wrap{height:32px}
  .ticker-item{padding:0 16px;font-size:11px}
  .nl{padding:32px 20px}
  .nl-f{flex-direction:column}
  .vi-grid{grid-template-columns:1fr 1fr}
  .sig-src{min-width:60px}
  .sig-time{min-width:44px}
}

@media(max-width:768px){.matrix{display:flex!important;flex-direction:column!important}#vidCol{order:-1!important}#podCol{order:0!important}.kol-col{order:1!important}}
</style>
{% endblock %}

{% block content %}
<div class="mh">

<!-- ═══════════ TICKER HERO ═══════════ -->
<div class="ticker-hero">

  <!-- Scrolling Ticker Bar -->
  <div class="ticker-wrap">
    <div class="ticker-track" id="tickerTrack">
      {% if ticker_items %}
        {% for t in ticker_items %}{% for _ in range(2) %}
        <a class="ticker-item" href="{{ t.url }}" target="_blank" rel="noopener">
          <span class="ticker-src">{{ t.source }}</span>
          <span class="ticker-title">{{ t.title }}</span>
          {% if t.score >= 70 %}<span class="ticker-score high">{{ t.score }}</span>
          {% elif t.score >= 40 %}<span class="ticker-score mid">{{ t.score }}</span>
          {% else %}<span class="ticker-score low">{{ t.score }}</span>{% endif %}
          {% if t.time %}<span class="ticker-time">{{ t.time }}</span>{% endif %}
        </a>
        <div class="ticker-sep"></div>
        {% endfor %}{% endfor %}
      {% else %}
        <span class="ticker-item"><span class="ticker-src">SYNCING</span><span class="ticker-title">Connecting to feeds...</span></span>
      {% endif %}
    </div>
  </div>

  <!-- Hero Content -->
  <div class="hero-content">
    <div class="hero-bg"><div class="hero-orb hero-orb-1"></div><div class="hero-orb hero-orb-2"></div></div>
    <div class="hero-tag"><div class="hero-tag-dot"></div><span class="hero-tag-text">Bitcoin Media Command Center</span></div>
    <h1 class="hero-h">Every <em>Voice</em>. Every <em>Signal</em>.</h1>
    <p class="hero-sub"><span id="hero-feed-count">17</span> feeds aggregated. Live podcasts, video intelligence, and KOL signals — one screen.</p>
    <div class="hero-stats">
      <div class="hero-stat"><div class="hero-stat-val mono"><span id="stat-feeds">{{ feed_stats.get('feed_count', 0) }}</span></div><div class="hero-stat-lab">Feeds</div></div>
      <div class="hero-stat"><div class="hero-stat-val mono"><span id="stat-podcasts">{{ feed_stats.get('podcast_count', 0) }}</span></div><div class="hero-stat-lab">Podcasts</div></div>
      <div class="hero-stat"><div class="hero-stat-val mono"><span id="stat-videos">{{ feed_stats.get('video_count', 0) }}</span></div><div class="hero-stat-lab">Videos</div></div>
      <div class="hero-stat"><div class="hero-stat-val mono" id="kolCount">{{ feed_matrix.kol|length if feed_matrix.kol else 0 }}</div><div class="hero-stat-lab">KOL Signals</div></div>
    </div>
  </div>
</div>

<!-- ═══════════ THREE-COLUMN FEED MATRIX ═══════════ -->
<div class="wrap">
  <div class="matrix">

    <!-- ── COLUMN 1: PODCASTS ── -->
    <div class="matrix-col" id="podCol">
      <div class="matrix-head">
        <div class="matrix-head-icon pod"><i class="fas fa-microphone-alt"></i></div>
        <span class="matrix-head-title">Podcasts</span>
        <span class="matrix-head-count">{{ feed_matrix.podcasts|length if feed_matrix.podcasts else 0 }}</span>
      </div>
      <div class="matrix-feed" id="podFeed">
        {% if feed_matrix.podcasts %}
          {# Sort: Protocol Pulse native episodes first, then partners #}
          {% set native_eps = feed_matrix.podcasts | selectattr('source', 'defined') | selectattr('source', 'equalto', 'Protocol Pulse') | list %}
          {% set partner_eps = feed_matrix.podcasts | rejectattr('source', 'defined') | list + (feed_matrix.podcasts | selectattr('source', 'defined') | rejectattr('source', 'equalto', 'Protocol Pulse') | list) %}
          {% set ordered_eps = native_eps + partner_eps %}
          {% for ep in ordered_eps if ep %}
          <a class="fi" href="{{ ep.source_url or '#' }}" target="_blank" rel="noopener" data-voice="{{ ep.feed_name|lower }}" {% if ep.audio_url %}onclick="event.preventDefault();playEp('{{ ep.audio_url }}','{{ ep.title|e }}','{{ ep.feed_name|e }}')"{% endif %}>
            {% if ep.published_at %}<div class="fi-new" data-pub="{{ ep.published_at }}"><div class="fi-new-dot"></div>NEW</div>{% endif %}
            <div class="fi-row">
              {% if ep.thumbnail_url %}<img class="fi-thumb" src="{{ ep.thumbnail_url }}" alt="" loading="lazy" onerror="this.style.display='none'">{% endif %}
              <div class="fi-info">
                <div class="fi-show pod">{{ ep.feed_name }}</div>
                <div class="fi-title">{{ ep.title }}</div>
                <div class="fi-meta">
                  {% if ep.duration %}<span class="fi-dur"><i class="far fa-clock"></i> {{ ep.duration }}</span>{% endif %}
                  {% if ep.published_at %}<span class="fi-time">{{ ep.published_at[:10] }}</span>{% endif %}
                  {% if ep.signal_score >= 70 %}<span class="fi-score high">{{ ep.signal_score }}</span>
                  {% elif ep.signal_score >= 40 %}<span class="fi-score mid">{{ ep.signal_score }}</span>
                  {% elif ep.signal_score > 0 %}<span class="fi-score low">{{ ep.signal_score }}</span>{% endif %}
                </div>
                {% if ep.summary_ai %}<div class="fi-summary">{{ ep.summary_ai }}</div>{% endif %}
              </div>
            </div>
            <div class="fi-wave" data-seed="{{ ep.title }}"></div>
          </a>
          {% endfor %}
        {% else %}
          <div class="fi-empty">
            <i class="fas fa-satellite-dish" style="font-size:20px;opacity:.3;display:block;margin-bottom:10px"></i>
            Syncing podcast feeds...<br><div class="kol-loader"></div>
          </div>
        {% endif %}
      </div>
    </div>

    <!-- ── COLUMN 2: VIDEO INTEL (Series Cards) ── -->
    <div class="matrix-col" id="vidCol">
      <div class="matrix-head">
        <div class="matrix-head-icon vid"><i class="fas fa-play"></i></div>
        <span class="matrix-head-title">Video Intel</span>
        <span class="matrix-head-count">{{ feed_matrix.videos|length if feed_matrix.videos else 0 }}</span>
      </div>
      <div class="matrix-feed" id="vidFeed">
        {% if feed_matrix.videos %}
          <div class="vi-grid">
          {% for ep in feed_matrix.videos %}
          <div class="vi-card" onclick="openVideoModal('{{ ep.video_url|e }}','{{ ep.title|e }}','{{ ep.feed_name|e }}','{{ ep.published_at[:10] if ep.published_at else '' }}','{{ ep.summary_ai|e if ep.summary_ai else '' }}')">
            <div class="vi-img-w">
              {% if ep.thumbnail_url %}
              <img class="vi-img" src="{{ ep.thumbnail_url }}" alt="" loading="lazy" onerror="this.style.display='none'">
              {% endif %}
              <div class="vi-ov"></div>
              <div class="vi-play"><i class="fas fa-play"></i></div>
              {% if ep.duration %}<div class="vi-dur">{{ ep.duration }}</div>{% endif %}
              {% if ep.signal_score > 0 %}<div class="vi-score">{{ ep.signal_score }}</div>{% endif %}
              {% if ep.published_at %}
              <div class="vi-new" data-pub="{{ ep.published_at }}" style="display:none">NEW</div>
              {% endif %}
            </div>
            <div class="vi-body">
              <div class="vi-src">{{ ep.feed_name }}</div>
              <div class="vi-title">{{ ep.title }}</div>
            </div>
          </div>
          {% endfor %}
          </div>
        {% else %}
          <div class="fi-empty">
            <i class="fas fa-video" style="font-size:20px;opacity:.3;display:block;margin-bottom:10px"></i>
            Syncing video channels...<br><div class="kol-loader"></div>
          </div>
        {% endif %}
      </div>
    </div>

    <!-- ── COLUMN 3: KOL SIGNALS ── -->
    <div class="matrix-col kol-col">
      <div class="matrix-head">
        <div class="matrix-head-icon kol"><i class="fas fa-bolt"></i></div>
        <span class="matrix-head-title">KOL Intel</span>
        <span class="matrix-head-count">{{ feed_matrix.kol|length if feed_matrix.kol else 0 }}</span>
      </div>
      <div class="matrix-feed" id="kolFeed">
        {% if feed_matrix.kol %}
          {% for item in feed_matrix.kol %}
          <a class="fi" href="{{ item.source_url or item.tweet_url or item.url or ('/articles/' ~ item.slug if item.slug else ('/intelligence' if item.type == 'signal' else '#')) }}" target="_blank" rel="noopener" style="cursor:pointer;text-decoration:none;color:inherit">
            <div class="fi-info">
              <div class="fi-show kol">{{ item.source }}</div>
              <div class="fi-title">{{ item.title }}</div>
              <div class="fi-meta">
                {% if item.timestamp %}<span class="fi-time">{{ item.timestamp[:10] if item.timestamp|length > 10 else item.timestamp }}</span>{% endif %}
                <span class="fi-dir {{ item.direction }}">{{ item.direction }}</span>
              </div>
              <div class="fi-summary">{{ item.excerpt }}</div>
            </div>
          </a>
          {% endfor %}
        {% else %}
          <div class="fi-empty">
            <i class="fas fa-bolt" style="font-size:20px;opacity:.3;display:block;margin-bottom:10px"></i>
            No KOL signals yet
          </div>
        {% endif %}
      </div>
    </div>

  </div><!-- /matrix -->

  <!-- ═══════════ NETWORK FEED ═══════════ -->
  <div class="feed-duo">

    <!-- ── What the Network Is Saying ── -->
    <div class="feed-section">
      <div class="sec-head">
        <div class="sec-head-icon net"><i class="fas fa-satellite-dish"></i></div>
        <span class="sec-head-title">What the Network Is Saying</span>
        <span class="sec-head-updated" id="netFeedTs">Loading...</span>
      </div>
      <div class="feed-list" id="networkFeed">
        <div style="text-align:center;padding:32px;color:var(--mut);font-size:12px">
          <i class="fas fa-satellite-dish" style="font-size:18px;opacity:.3;display:block;margin-bottom:8px"></i>
          Syncing network signals...<br><div class="kol-loader"></div>
        </div>
      </div>
    </div>

  </div>

  <!-- Live signal terminal removed from /media — use /terminal instead -->
  <div id="books" style="padding:48px 0 0;">
    <!-- Header -->
    <div style="text-align:center;margin-bottom:2rem;">
      <div style="display:inline-flex;align-items:center;gap:0.6rem;background:rgba(220,38,38,0.08);border:1px solid rgba(220,38,38,0.2);padding:0.5rem 1.4rem;border-radius:999px;color:var(--red);font-family:'JetBrains Mono',monospace;font-size:0.7rem;font-weight:500;letter-spacing:0.15em;text-transform:uppercase;margin-bottom:1rem;"><i class="fas fa-chart-line"></i> SOVEREIGN LIBRARY</div>
      <h2 style="font-size:clamp(1.8rem,4vw,2.6rem);font-weight:800;color:var(--bright);letter-spacing:-0.03em;margin-bottom:0.5rem;">Live Book Metrics</h2>
      <p style="font-size:0.95rem;color:var(--mut);max-width:600px;margin:0 auto;line-height:1.5;">Bloomberg terminal for sovereign money books. Bubble size = sales velocity. Position = recency vs. rank momentum.</p>
      <div style="font-family:'JetBrains Mono',monospace;font-size:0.7rem;color:var(--mut);margin-top:0.5rem;"><span style="display:inline-block;width:6px;height:6px;background:#22c55e;border-radius:50%;margin-right:4px;animation:tagPulse 2s infinite;vertical-align:middle;"></span> <span id="bookMetricsUpdated">Loading...</span></div>
    </div>

    <!-- Featured Book Banner -->
    <div id="featuredBookBanner" style="display:none;"></div>

    <!-- Bubble Chart -->
    <div style="background:var(--card);border:1px solid var(--border);border-radius:12px;padding:1.5rem;margin-bottom:2.5rem;">
      <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:1rem;padding-bottom:0.75rem;border-bottom:1px solid var(--border);">
        <span style="font-family:'JetBrains Mono',monospace;font-size:0.75rem;color:var(--mut);text-transform:uppercase;letter-spacing:0.1em;"><i class="fas fa-circle" style="color:#22c55e;font-size:6px;vertical-align:middle;margin-right:4px;"></i> SALES VELOCITY MAP</span>
        <div style="display:flex;gap:1rem;font-size:0.7rem;font-family:'JetBrains Mono',monospace;color:var(--mut);">
          <span><span style="display:inline-block;width:8px;height:8px;border-radius:50%;background:#22c55e;margin-right:4px;vertical-align:middle;"></span>Rising</span>
          <span><span style="display:inline-block;width:8px;height:8px;border-radius:50%;background:#f7931a;margin-right:4px;vertical-align:middle;"></span>Stable</span>
          <span><span style="display:inline-block;width:8px;height:8px;border-radius:50%;background:#dc2626;margin-right:4px;vertical-align:middle;"></span>PP Series</span>
        </div>
      </div>
      <div style="position:relative;height:420px;"><canvas id="bookBubbleChart"></canvas></div>
    </div>

    <!-- Rising Stars -->
    <div id="risingStarsSection" style="display:none;margin-bottom:2.5rem;">
      <div style="font-family:'JetBrains Mono',monospace;font-size:0.8rem;color:var(--mut);text-transform:uppercase;letter-spacing:0.1em;margin-bottom:1rem;display:flex;align-items:center;gap:0.5rem;"><i class="fas fa-arrow-trend-up" style="color:#22c55e;"></i> RISING STARS — BSR improved &gt;10% in 24h</div>
      <div id="risingStarsGrid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:1rem;"></div>
    </div>

    <!-- Bestsellers Grid -->
    <div style="margin-top:2rem;">
      <div style="display:flex;align-items:center;gap:0.75rem;margin-bottom:1.5rem;">
        <span style="font-family:'JetBrains Mono',monospace;font-size:0.8rem;color:var(--mut);text-transform:uppercase;letter-spacing:0.1em;"><i class="fab fa-amazon" style="color:var(--btc);margin-right:4px;"></i> SOVEREIGN MONEY BESTSELLERS</span>
        <span id="bestsellersCount" style="font-family:'JetBrains Mono',monospace;font-size:0.65rem;color:var(--btc);background:rgba(247,147,26,0.1);border:1px solid rgba(247,147,26,0.2);padding:0.15rem 0.5rem;border-radius:4px;">0 BOOKS</span>
      </div>
      <div id="bestsellersGrid" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(180px,1fr));gap:1.25rem;"></div>
    </div>
  </div>

</div><!-- /wrap -->

</div><!-- /mh -->

<!-- ═══════════ AUDIO PLAYER BAR ═══════════ -->
<div class="abar" id="audioBar">
  <button class="abar-btn" id="abPlayPause" onclick="togglePlayPause()"><i class="fas fa-pause"></i></button>
  <div class="abar-now">
    <div class="abar-show" id="abShow"></div>
    <div class="abar-title" id="abTitle"></div>
    <div class="abar-progress" onclick="seekAudio(event)"><div class="abar-progress-fill" id="abProgress"></div></div>
  </div>
  <button class="abar-close" onclick="closeAudio()"><i class="fas fa-times"></i></button>
</div>
<audio id="audioEl" preload="none"></audio>

<!-- ═══════════ VIDEO MODAL ═══════════ -->
<div class="vm-overlay" id="videoModal" onclick="closeVideoModal(event)">
  <div style="max-width:960px;width:90vw" onclick="event.stopPropagation()">
    <div class="vm-wrap">
      <button class="vm-close" onclick="closeVideoModal(event,true)"><i class="fas fa-times"></i></button>
      <iframe id="vmIframe" allow="autoplay;fullscreen" allowfullscreen></iframe>
    </div>
    <div id="vmMeta" style="padding:16px 4px 0;display:none">
      <div id="vmTitle" style="font-size:15px;font-weight:700;color:var(--bright,#eef2ff);margin-bottom:6px"></div>
      <div style="display:flex;gap:10px;align-items:center">
        <span id="vmSource" style="font-size:12px;color:var(--mut,#95a0ba)"></span>
        <span id="vmDate" style="font-size:11px;background:rgba(255,255,255,0.06);border:1px solid rgba(255,255,255,0.1);border-radius:4px;padding:2px 8px;color:var(--mut,#95a0ba);font-family:'JetBrains Mono',monospace"></span>
      </div>
      <div id="vmSummary" style="font-size:13px;color:var(--mut,#95a0ba);margin-top:8px;line-height:1.5;max-height:60px;overflow:hidden"></div>
    </div>
  </div>
</div>

{% endblock %}

{% block scripts %}
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>
<script>
/* ═══════════ AUDIO PLAYER ═══════════ */
var _audio=document.getElementById('audioEl');
var _playing=false;

function playEp(url,title,show){
  if(!url)return;
  _audio.src=url;_audio.play();_playing=true;
  document.getElementById('abTitle').textContent=title;
  document.getElementById('abShow').textContent=show;
  document.getElementById('audioBar').classList.add('active');
  document.getElementById('abPlayPause').innerHTML='<i class="fas fa-pause"></i>';
}
function togglePlayPause(){
  if(!_audio.src)return;
  if(_playing){_audio.pause();_playing=false;document.getElementById('abPlayPause').innerHTML='<i class="fas fa-play"></i>'}
  else{_audio.play();_playing=true;document.getElementById('abPlayPause').innerHTML='<i class="fas fa-pause"></i>'}
}
function closeAudio(){
  _audio.pause();_audio.src='';_playing=false;
  document.getElementById('audioBar').classList.remove('active');
}
function seekAudio(e){
  if(!_audio.duration)return;
  var rect=e.currentTarget.getBoundingClientRect();
  var pct=(e.clientX-rect.left)/rect.width;
  _audio.currentTime=pct*_audio.duration;
}
_audio.addEventListener('timeupdate',function(){
  if(!_audio.duration)return;
  document.getElementById('abProgress').style.width=((_audio.currentTime/_audio.duration)*100)+'%';
});

/* ═══════════ VIDEO MODAL ═══════════ */
function openVideoModal(url,title,source,date,summary){
  var vid=url;
  var m=url.match(/(?:youtu\.be\/|youtube\.com\/(?:watch\?v=|embed\/|v\/))([A-Za-z0-9_-]{11})/);
  if(m) vid=m[1];
  if(!vid)return;
  document.getElementById('vmIframe').src='https://www.youtube.com/embed/'+vid+'?autoplay=1';
  document.getElementById('videoModal').classList.add('active');
  document.body.style.overflow='hidden';
  var meta=document.getElementById('vmMeta');
  if(title||source||date){
    meta.style.display='block';
    document.getElementById('vmTitle').textContent=title||'';
    document.getElementById('vmSource').textContent=source||'';
    document.getElementById('vmDate').textContent=date||'';
    document.getElementById('vmSummary').textContent=summary||'';
  } else { meta.style.display='none'; }
}
function closeVideoModal(e,force){
  if(!force&&e&&e.target!==document.getElementById('videoModal'))return;
  document.getElementById('vmIframe').src='';
  document.getElementById('videoModal').classList.remove('active');
  document.body.style.overflow='';
  document.getElementById('vmMeta').style.display='none';
}
document.addEventListener('keydown',function(e){if(e.key==='Escape')closeVideoModal(e,true)});

/* ═══════════ WAVEFORM BARS ═══════════ */
document.querySelectorAll('.fi-wave').forEach(function(el){
  var seed=el.getAttribute('data-seed')||'';
  var h=0;for(var i=0;i<seed.length;i++){h=((h<<5)-h)+seed.charCodeAt(i);h|=0}
  for(var b=0;b<25;b++){
    var bar=document.createElement('div');
    bar.className='fi-wave-bar';
    var rng=Math.abs(Math.sin(h*0.01*(b+1)))*0.8+0.2;
    bar.style.height=Math.round(rng*16+2)+'px';
    el.appendChild(bar);
  }
});

/* ═══════════ NEW BADGE (7-day window for video intel, 48h for podcasts) ═══════════ */
(function(){
  var now=Date.now();
  document.querySelectorAll('.fi-new[data-pub]').forEach(function(el){
    var pub=el.getAttribute('data-pub');if(!pub)return;
    var d=new Date(pub);
    el.style.display=(!isNaN(d)&&(now-d.getTime())<48*3600*1000)?'flex':'none';
  });
  document.querySelectorAll('.vi-new[data-pub]').forEach(function(el){
    var pub=el.getAttribute('data-pub');if(!pub)return;
    var d=new Date(pub);
    el.style.display=(!isNaN(d)&&(now-d.getTime())<7*24*3600*1000)?'inline-block':'none';
  });
})();

/* ═══════════ AUTO-SYNC ON EMPTY ═══════════ */
(function(){
  var podF=document.getElementById('podFeed');
  if(podF&&podF.querySelector('.kol-loader')){
    fetch('/api/media/sync',{method:'POST'}).catch(function(){});
    setTimeout(function poll(){
      fetch('/api/media/matrix').then(function(r){return r.json()}).then(function(d){
        if(d.podcasts&&d.podcasts.length>0)location.reload();
        else setTimeout(poll,15000);
      }).catch(function(){setTimeout(poll,30000)});
    },20000);
  }
})();

/* ═══════════ NETWORK FEED (auto-refresh 30min) ═══════════ */
function _timeAgo(ts){
  if(!ts)return '';
  var d=new Date(ts);if(isNaN(d))return '';
  var s=Math.floor((Date.now()-d.getTime())/1000);
  if(s<60)return s+'s ago';
  if(s<3600)return Math.floor(s/60)+'m ago';
  if(s<86400)return Math.floor(s/3600)+'h ago';
  return Math.floor(s/86400)+'d ago';
}
function _fmtTime(ts){
  if(!ts)return '';
  var d=new Date(ts);if(isNaN(d))return '';
  return d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',hour12:false});
}

function loadNetworkFeed(){
  fetch('/api/media/network-feed').then(function(r){return r.json()}).then(function(d){
    var el=document.getElementById('networkFeed');
    var tsEl=document.getElementById('netFeedTs');
    if(!d.items||!d.items.length){
      el.innerHTML='<div style="text-align:center;padding:32px;color:var(--mut);font-size:12px">No network signals yet</div>';
      return;
    }
    var html='';
    d.items.forEach(function(item){
      html+='<div class="nfi">';
      html+='<div class="nfi-icon"><i class="'+(item.icon||'fas fa-hashtag')+'"></i></div>';
      html+='<div class="nfi-body">';
      html+='<div class="nfi-source"><span class="nfi-sent '+item.sentiment+'"></span>'+_esc(item.source)+' &middot; '+_esc(item.source_type)+'</div>';
      html+='<div class="nfi-text">'+_esc(item.text)+'</div>';
      html+='<div class="nfi-time">'+_timeAgo(item.timestamp)+'</div>';
      html+='</div></div>';
    });
    el.innerHTML=html;
    if(tsEl)tsEl.textContent='Updated '+_timeAgo(d.updated_at);
  }).catch(function(){});
}

function loadLiveSignals(){
  fetch('/api/media/live-signals').then(function(r){return r.json()}).then(function(d){
    var el=document.getElementById('signalFeed');
    var tsEl=document.getElementById('sigTermTs');
    if(!d.signals||!d.signals.length){
      el.innerHTML='<div style="text-align:center;padding:40px;color:var(--mut);font-size:12px">No active signals</div>';
      return;
    }
    var html='';
    d.signals.forEach(function(sig){
      var dir=sig.direction||'neutral';
      var str=sig.strength||1;
      html+='<div class="sig-item '+dir+'">';
      html+='<div class="sig-time">'+_fmtTime(sig.timestamp)+'</div>';
      html+='<div class="sig-src '+dir+'">'+_esc(sig.source)+'</div>';
      html+='<div class="sig-text">'+_esc(sig.text)+'</div>';
      html+='<div class="sig-strength">';
      for(var i=1;i<=4;i++){
        html+='<div class="sig-strength-bar'+(i<=str?' active':'')+'"></div>';
      }
      html+='</div></div>';
    });
    el.innerHTML=html;
    if(tsEl)tsEl.textContent='Live \u2022 '+new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',hour12:false});
  }).catch(function(){});
}

function _esc(s){if(!s)return '';var d=document.createElement('div');d.textContent=s;return d.innerHTML;}

// Initial load
loadNetworkFeed();
loadLiveSignals();

// Auto-refresh: network every 30min, signals every 60s
setInterval(loadNetworkFeed,30*60*1000);
setInterval(loadLiveSignals,60*1000);

/* ═══════════ NEWSLETTER ═══════════ */
function mhNlSub(e){
  e.preventDefault();
  var em=e.target.querySelector("input[type=email]");
  if(!em||!em.value)return;
  var btn=e.target.querySelector("button");
  if(btn)btn.textContent="...";
  fetch("/api/newsletter/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:em.value})})
  .then(function(r){return r.json()}).then(function(d){
    if(d.success||String(d.message||"").toLowerCase().includes("subscrib")){
      if(btn){btn.textContent="Done!";btn.style.background="#22c55e";}em.value="";
      setTimeout(function(){if(btn){btn.textContent="Subscribe";btn.style.background="";}},3000);
    }else{alert(d.message||"Error");if(btn)btn.textContent="Subscribe";}
  }).catch(function(){if(btn)btn.textContent="Subscribe";});
}

/* ═══════════ SOVEREIGN BOOK LIBRARY ═══════════ */
(function(){
    var bookChart=null;
    var PP_ASINS=['B0DVTCVX8J','9916697191','0241360846','B0CQLMQRH7'];

    function fetchBookMetrics(){
        fetch('/api/books/metrics').then(function(r){return r.json()}).then(function(data){
            renderFeaturedBook(data.featured);
            renderBubbleChart(data.books);
            renderRisingStars(data.rising);
            renderBestsellersGrid(data.books);
            if(data.last_updated){
                var d=new Date(data.last_updated);
                var el=document.getElementById('bookMetricsUpdated');
                if(el) el.textContent='Updated '+d.toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})+' '+d.toLocaleTimeString('en-US',{hour:'2-digit',minute:'2-digit'});
            }
        }).catch(function(e){console.warn('Book metrics:',e)});
    }

    function renderFeaturedBook(f){
        var el=document.getElementById('featuredBookBanner');
        if(!f||!el){if(el)el.style.display='none';return;}
        el.style.display='block';
        el.innerHTML='<div style="display:grid;grid-template-columns:auto 1fr;gap:2rem;background:linear-gradient(135deg,var(--card) 0%,rgba(220,38,38,0.06) 100%);border:1px solid rgba(220,38,38,0.2);border-radius:12px;padding:2rem;margin-bottom:2.5rem;position:relative;overflow:hidden;border-left:4px solid var(--red);">'+
            '<div style="width:140px;height:200px;border-radius:8px;overflow:hidden;box-shadow:0 8px 32px rgba(0,0,0,0.4);flex-shrink:0;"><img src="'+_esc(f.cover_url)+'" alt="'+_esc(f.title)+'" style="width:100%;height:100%;object-fit:cover;" loading="lazy"></div>'+
            '<div style="display:flex;flex-direction:column;justify-content:center;">'+
                '<div style="display:inline-flex;align-items:center;gap:0.4rem;background:var(--red);color:#fff;font-family:JetBrains Mono,monospace;font-size:0.65rem;font-weight:700;padding:0.3rem 0.8rem;border-radius:4px;text-transform:uppercase;letter-spacing:0.08em;margin-bottom:0.75rem;width:fit-content;"><i class="fas fa-star"></i> '+_esc(f.badge||'FEATURED')+'</div>'+
                '<div style="font-size:1.4rem;font-weight:700;color:var(--bright);margin-bottom:0.5rem;letter-spacing:-0.02em;">'+_esc(f.headline||f.title)+'</div>'+
                '<div style="font-size:0.9rem;color:var(--sec);line-height:1.6;margin-bottom:1rem;">'+_esc(f.body_text||'')+'</div>'+
                '<div style="display:flex;gap:0.75rem;flex-wrap:wrap;">'+
                    '<a href="'+_esc(f.amazon_url)+'" target="_blank" rel="noopener" style="display:inline-flex;align-items:center;gap:0.5rem;background:var(--red);color:#fff;padding:0.7rem 1.5rem;border-radius:8px;text-decoration:none;font-weight:600;font-size:0.85rem;"><i class="fab fa-amazon"></i> Get on Amazon</a>'+
                    (f.series_url?'<a href="'+_esc(f.series_url)+'" style="display:inline-flex;align-items:center;gap:0.5rem;background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.15);color:#fff;padding:0.7rem 1.5rem;border-radius:8px;text-decoration:none;font-weight:600;font-size:0.85rem;"><i class="fas fa-play-circle"></i> Watch the Series</a>':'')+
                    '<a href="https://www.youtube.com/@ProtocolPulse" target="_blank" rel="noopener" style="display:inline-flex;align-items:center;gap:0.5rem;background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.15);color:#fff;padding:0.7rem 1.5rem;border-radius:8px;text-decoration:none;font-weight:600;font-size:0.85rem;"><i class="fab fa-youtube"></i> Big Print Series</a>'+
                '</div>'+
            '</div></div>';
    }

    function renderBubbleChart(books){
        if(!books||!books.length)return;
        var canvas=document.getElementById('bookBubbleChart');
        if(!canvas)return;
        var ctx=canvas.getContext('2d');
        var PP_ASINS=['B0DVTCVX8J','9916697191','0241360846','B0CQLMQRH7'];
        var ppBooks=[],risingBooks=[],stableBooks=[];
        books.forEach(function(b){
            if(PP_ASINS.indexOf(b.asin)>=0)ppBooks.push(b);
            else if(b.is_rising)risingBooks.push(b);
            else stableBooks.push(b);
        });
        function mkPts(arr){
            return arr.map(function(b){
                var bsr=b.bsr||50000;
                // Log-scale bubble: lower BSR = bigger bubble
                var r=Math.max(6,Math.min(32,32-Math.log10(bsr)*5.5));
                // X: BSR momentum — negative=improving, positive=declining
                var x=Math.max(-42,Math.min(22,b.bsr_change||0));
                // Y: velocity score 0-100
                var y=b.velocity||50;
                return {x:x,y:y,r:r,book:b};
            });
        }
        var datasets=[
            {label:'PP Series',data:mkPts(ppBooks),backgroundColor:'rgba(220,38,38,0.55)',borderColor:'rgba(220,38,38,0.9)',borderWidth:2,hoverBackgroundColor:'rgba(220,38,38,0.8)'},
            {label:'Rising',data:mkPts(risingBooks),backgroundColor:'rgba(34,197,94,0.45)',borderColor:'rgba(34,197,94,0.8)',borderWidth:2,hoverBackgroundColor:'rgba(34,197,94,0.7)'},
            {label:'Stable',data:mkPts(stableBooks),backgroundColor:'rgba(247,147,26,0.35)',borderColor:'rgba(247,147,26,0.6)',borderWidth:2,hoverBackgroundColor:'rgba(247,147,26,0.55)'}
        ];
        if(bookChart)bookChart.destroy();
        bookChart=new Chart(ctx,{
            type:'bubble',data:{datasets:datasets},
            options:{
                responsive:true,maintainAspectRatio:false,
                animation:{duration:1200,easing:'easeOutQuart'},
                scales:{
                    x:{
                        title:{display:true,text:'BSR MOMENTUM',color:'rgba(255,255,255,0.4)',font:{family:'JetBrains Mono',size:10,weight:'600'}},
                        min:-45,max:25,
                        grid:{color:'rgba(255,255,255,0.05)',drawBorder:false},
                        ticks:{color:'rgba(255,255,255,0.4)',font:{family:'JetBrains Mono',size:9},
                            callback:function(v){
                                if(v===-40)return 'HOT';
                                if(v===-20)return 'RISING';
                                if(v===0)return 'STABLE';
                                if(v===20)return 'COOLING';
                                return '';
                            }
                        }
                    },
                    y:{
                        title:{display:true,text:'SALES VELOCITY',color:'rgba(255,255,255,0.4)',font:{family:'JetBrains Mono',size:10,weight:'600'}},
                        min:0,max:105,
                        grid:{color:'rgba(255,255,255,0.05)',drawBorder:false},
                        ticks:{color:'rgba(255,255,255,0.4)',font:{family:'JetBrains Mono',size:9}}
                    }
                },
                plugins:{
                    legend:{
                        display:true,
                        position:'top',
                        labels:{color:'rgba(255,255,255,0.6)',font:{family:'JetBrains Mono',size:9},boxWidth:10,padding:16}
                    },
                    tooltip:{
                        backgroundColor:'#0d0d0d',
                        borderColor:'rgba(255,255,255,0.12)',borderWidth:1,
                        titleFont:{family:'JetBrains Mono',size:12,weight:'700'},
                        bodyFont:{family:'JetBrains Mono',size:10},
                        titleColor:'#fff',bodyColor:'rgba(255,255,255,0.65)',
                        padding:14,cornerRadius:8,
                        callbacks:{
                            title:function(items){return items[0].raw.book.title;},
                            label:function(item){
                                var b=item.raw.book;
                                var lines=['by '+b.author,'BSR: #'+b.bsr.toLocaleString()];
                                if(b.bsr_change)lines.push('Momentum: '+(b.bsr_change>0?'+':'')+b.bsr_change+'%');
                                if(b.is_rising)lines.push('RISING STAR');
                                if(b.is_breakout)lines.push('INITIAL BREAKOUT');
                                return lines;
                            }
                        }
                    }
                },
                onClick:function(evt,elements){
                    if(elements.length>0){
                        var b=bookChart.data.datasets[elements[0].datasetIndex].data[elements[0].index].book;
                        if(b.amazon_url)window.open(b.amazon_url,'_blank');
                    }
                },
                onHover:function(evt,elements){evt.native.target.style.cursor=elements.length>0?'pointer':'default';}
            }
        });
    }


    function renderRisingStars(rising){
        var section=document.getElementById('risingStarsSection');
        var grid=document.getElementById('risingStarsGrid');
        if(!rising||!rising.length){if(section)section.style.display='none';return;}
        section.style.display='block';
        grid.innerHTML=rising.map(function(b){
            return '<a href="'+_esc(b.amazon_url)+'" target="_blank" rel="noopener" style="display:flex;gap:1rem;background:var(--card);border:1px solid var(--border);border-radius:12px;padding:1rem;text-decoration:none;color:inherit;transition:border-color 0.2s,transform 0.2s;" onmouseover="this.style.borderColor=\'rgba(34,197,94,0.4)\';this.style.transform=\'translateY(-3px)\'" onmouseout="this.style.borderColor=\'\';this.style.transform=\'\'">'+
                '<div style="width:55px;height:80px;border-radius:6px;overflow:hidden;flex-shrink:0;"><img src="'+_esc(b.cover_url)+'" alt="'+_esc(b.title)+'" style="width:100%;height:100%;object-fit:cover;" loading="lazy"></div>'+
                '<div style="display:flex;flex-direction:column;justify-content:center;min-width:0;">'+
                    '<div style="font-size:0.85rem;font-weight:600;color:var(--bright);margin-bottom:0.2rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">'+_esc(b.title)+'</div>'+
                    '<div style="font-size:0.72rem;color:var(--mut);margin-bottom:0.4rem;">'+_esc(b.author)+'</div>'+
                    '<div style="display:inline-flex;align-items:center;gap:0.3rem;font-family:JetBrains Mono,monospace;font-size:0.65rem;font-weight:600;color:#22c55e;background:rgba(34,197,94,0.1);border:1px solid rgba(34,197,94,0.2);padding:0.2rem 0.5rem;border-radius:4px;width:fit-content;"><i class="fas fa-arrow-trend-up"></i> RISING '+Math.abs(b.bsr_change).toFixed(1)+'%</div>'+
                    '<div style="font-family:JetBrains Mono,monospace;font-size:0.65rem;color:var(--mut);margin-top:0.25rem;">BSR #'+b.bsr.toLocaleString()+'</div>'+
                '</div></a>';
        }).join('');
    }

    function renderBestsellersGrid(books){
        var grid=document.getElementById('bestsellersGrid');
        var countEl=document.getElementById('bestsellersCount');
        if(!books||!books.length)return;
        var sorted=books.slice().sort(function(a,b){return a.bsr-b.bsr;});
        if(countEl)countEl.textContent=sorted.length+' BOOKS';
        grid.innerHTML=sorted.map(function(b,idx){
            return '<a href="'+_esc(b.amazon_url)+'" target="_blank" rel="noopener" style="background:var(--card);border:1px solid var(--border);border-radius:12px;overflow:hidden;text-decoration:none;color:inherit;transition:border-color 0.25s,transform 0.25s;display:flex;flex-direction:column;" onmouseover="this.style.borderColor=\'rgba(247,147,26,0.3)\';this.style.transform=\'translateY(-6px)\'" onmouseout="this.style.borderColor=\'\';this.style.transform=\'\'">'+
                '<div style="height:220px;background:var(--elevated);display:flex;align-items:center;justify-content:center;padding:12px;position:relative;">'+
                    '<span style="position:absolute;top:8px;left:8px;background:rgba(0,0,0,0.7);border:1px solid var(--border);color:var(--btc);font-family:JetBrains Mono,monospace;font-size:0.6rem;font-weight:700;padding:0.2rem 0.4rem;border-radius:4px;">#'+(idx+1)+'</span>'+
                    '<img src="'+_esc(b.cover_url)+'" alt="'+_esc(b.title)+'" style="max-width:100%;max-height:100%;width:auto;height:auto;object-fit:contain;" loading="lazy">'+
                '</div>'+
                '<div style="padding:1rem;flex:1;display:flex;flex-direction:column;">'+
                    '<div style="font-size:0.9rem;font-weight:600;color:var(--bright);margin-bottom:0.3rem;line-height:1.25;">'+_esc(b.title)+'</div>'+
                    '<div style="font-size:0.75rem;color:var(--red);font-family:JetBrains Mono,monospace;margin-bottom:0.5rem;">'+_esc(b.author)+'</div>'+
                    '<span style="font-size:0.75rem;color:var(--mut);margin-top:auto;display:flex;align-items:center;gap:0.35rem;"><i class="fab fa-amazon"></i> BSR #'+b.bsr.toLocaleString()+'</span>'+
                '</div></a>';
        }).join('');
    }

    // Init on page load + refresh every 60s
    fetchBookMetrics();
    setInterval(fetchBookMetrics,60000);
})();
</script>

<script>
// Fetch live media stats
fetch('/api/media/stats').then(function(r){return r.json()}).then(function(d){
  var f=document.getElementById('stat-feeds');
  var p=document.getElementById('stat-podcasts');
  var v=document.getElementById('stat-videos');
  if(f)f.textContent=d.feed_count||0; var hfc=document.getElementById('hero-feed-count'); if(hfc)hfc.textContent=d.feed_count||17;
  if(p)p.textContent=d.podcast_count||0;
  if(v)v.textContent=d.video_count||0;
}).catch(function(){});
</script>

{% endblock %}
